# 3.0.4 Phase 000 Plan Reconciliation

**Phase:** `CORE-PHASE-000`

**Requirement:** `CORE-REQ-001`

**Collected:** 2026-09-02

**Status:** baseline identity reconciled. This record supersedes the identity gate and the two row classifications in `3.0.4-phase-000-superseding-evidence.md`. It does not replace the immutable observations in `3.0.4-phase-000-audit.md`.

## 1. Reconciled Identities

| Role | Identity | Verification | Use |
| --- | --- | --- | --- |
| Frozen audit source | `5b3077764907249b3711886cca538794f6139acf` | Signed Git commit. It is the saved-goal checkout and parented by the earlier plan revision. | Historical code and artifact audit baseline. |
| Earlier plan provenance | `f6ceb9b49fda8b3cf764706bb1e5113b75383ebb` | Signed Git commit and direct parent of `5b30777`. | Historical plan-authoring provenance only. It is not a competing audit baseline. |
| Plan correction | `f6880c31c02a4efc617d548217760121570d649c` | Signed Git commit on `envy/polish-3.0.4-plan`. | Reconciles the authoritative plan, phase dependencies, evidence lifecycle, manifest, and handoff. |
| Authoritative repository | `https://github.com/MCEnvision/ProgressiveStages.git` | `origin` remote and live default ref inspection. | Project identity. |
| Live default branch at collection | `origin/master` at `ab4ad138e1f74eec82cf0392a47ac1e54dd66d01` | `git fetch origin --prune` and `git ls-remote`. | Approved implementation base, not a replacement for the frozen audit source. |

The earlier superseding record correctly identified an ambiguity, but the repaired plan resolves it without changing the saved goal. `5b30777` is the sole frozen source baseline for Phase 000 evidence. `f6ceb9` remains historical authoring provenance. The current phase branch contains the plan and evidence records needed to integrate the audit, but does not redefine the historical source under inspection.

The source, editor, Gradle, and user-facing documentation paths compared between `5b30777` and `f6880c3` have no runtime-code change. The only relevant differences are the Phase 000 evidence records and corrected plan artifacts. The baseline source seam observations therefore remain admissible.

## 2. Shipped Artifact Recheck

The pinned installed artifact is `/tmp/progressivestages-audit/progressivestages-3.0.3.jar`. Its hashes were recalculated during this reconciliation.

| Field | Value |
| --- | --- |
| Version | `3.0.3` |
| Minecraft | `1.21.1` |
| Loader | NeoForge |
| Size | `1,758,353` bytes |
| SHA-256 | `5c9a290a64629b33953d587a677c6bcf530ad523ad944b63c12d2750c8d1797a` |
| SHA-512 | `2d9561af1f982d1fb1f9678df248bc240c4f2247811cd4fe7ae73a69606a8d931220c5ba07ccf01dbb1ee67efda1fdcbb8f3e967ffbeb1df6d48f70fdde6dca2` |
| Packed editor `app.js` SHA-256 | `d0c966b1cfb094de6b9e31b5231e95eb6a20efb19497b96aadfa1e6760289576` |

The JAR contains the expected optional JEI and EMI metadata, `CuriosCompat`, `EntityPresenceEnforcer`, `StageTreeScreen`, `StageTreeInventoryButton`, the packed editor bundle, and the progression-map icon. It does not contain a source commit identity, so the binary is not cryptographically attributed to a source revision beyond its independent release identity and hashes.

## 3. Six Issue Classification Reconciliation

These remain the only report rows. `SRC-009` remains a separately typed owner request for `CORE-REQ-012` and is not a seventh issue.

| Issue | Classification | Frozen evidence | Downstream owner and required proof |
| --- | --- | --- | --- |
| `#8` Curios 9.5.1 | `artifact_verified` | The packed `CuriosCompat` resolves `top.theillusivec4.curios.api.type.inventory.ICuriosItemHandler`, while the report identifies the supported capability package. Failure falls back to item-level locking. | `CORE-REQ-003`, Phase 002. Prove Curios 9.5.1 present and absent startup, slot behavior, inventory conservation, and final JAR isolation. |
| `#10` JEI with EMI | `artifact_verified` | Packed `StageConfig` exposes the EMI setting only. The packed metadata declares JEI and EMI optional, but no independent JEI enablement surface was found. | `CORE-REQ-004`, Phase 002. Prove JEI only, EMI only, both, neither, enabled, disabled, missing-config, and reload behavior. |
| `#11` enchantment selection controls | `stale_with_evidence` | The packed editor contains `enchants.max_levels` and `enchants.selection_weights`. The prior matching-bundle operator record used the identical packed `app.js` hash, wrote both fields, persisted and reopened them. | `CORE-REQ-005`, Phase 003. Preserve the visual regression and prove table and loot generation behavior in the 3.0.4 candidate. |
| `#16` category overlay depth | `blocked_by_missing_evidence` | Source marker and static render-order test are insufficient. No supported-GUI-scale shipped-artifact client capture was retained. | `CORE-REQ-006`, Phase 003. Capture rendering and input behavior for menu open, close, selection, pan, zoom, search, navigation, and inspector behavior. |
| `#24` entity presence cost | `artifact_verified` | Packed tracking code reaches `CompiledRuleEngine.resolveUntracked`; packed rule resolution constructs a `MinecraftConditionContextFactory` context before candidate evaluation. | `CORE-REQ-002`, Phase 001. Prove one context per player, tick, and authoritative-state revision with mixed-player correctness and the approved performance gate. |
| `#25` visual recipe locks | `reproduced` | The identical packed editor bundle's prior operator workflow persisted a generic `[[rules]]` recipe target after visual authoring. The packed bundle has no `locked_items` or `locked_ids` token, while the parser's canonical fields are distinct. | `CORE-REQ-011`, Phase 003. Prove output-item and exact-recipe authoring separately, atomic legacy rejection or migration, reload, compiler activation, and denied and eligible runtime crafting. |

The `#25` reproduction proves the observed noncanonical visual serialization. It does not prove the separate output-item selector, corrected canonical persistence, reload, or player crafting behavior. Those are deliberately retained as Phase 003 acceptance gates.

## 4. Current Seam Confirmation

The current baseline inventory interaction surface remains observational and unchanged from the prior packet:

- `StageFileParser.parseInteractions` accepts the legacy `item_on_block`, `block_right_click`, and `item_on_entity` forms.
- `LockDefinition.InteractionLock` contains only `type`, `heldItem`, `target`, and `description`.
- `Schema4StageCompiler.addGenericRules` and the existing selector registry preserve single-category selector expansion, the `all`, `id`, `mod`, `tag`, `name`, and `#` grammar, priority exceptions, and safe ties.
- `AbstractContainerMenuMixin` gates existing movement but has no authoritative destination inventory-owner resolver or paired insertion decision.
- `BuiltinEditorSchemas` and `RulesPanel.tsx` expose legacy interaction choices and a generic visual-rule surface. They do not provide the paired source and destination contract required by `CORE-REQ-012`.

No `InventoryTargetResolver`, `InventoryTargetResolverRegistry`, `InventoryOwnerTarget`, or `ContainerTargetCatalog` implementation exists in the frozen source. Phase 003 must add only the planned `item_into_inventory` extension while preserving legacy interactions and playerless automation.

## 5. External Boundary and Transition Result

| Boundary | State | Phase 000 result |
| --- | --- | --- |
| `EXT-001` public Curios, JEI, and EMI artifacts | available but not yet runtime-verified | Reserved for Phase 002. |
| `EXT-002` shared release-validation workflow revision | unavailable | Preserved as a Phase 005 and 006 blocker. |
| `EXT-003` final platform publication authorization | unknown and unauthorized | Preserved as a Phase 006 boundary. |

The former identity conflict is resolved. Phase 000 still requires its final packet consistency review, tracked evidence commit, pull request checks and review, merge into `master`, and a signed Phase 000 tag before Phase 001 may begin. No corrective runtime source change is authorized by this reconciliation record.
