# 3.0.4 Phase 000 Superseding Evidence

**Phase:** `CORE-PHASE-000`

**Requirement:** `CORE-REQ-001`

**Collected:** 2026-09-02
**Status:** incomplete. This record supersedes only the completion conclusion in `3.0.4-phase-000-audit.md`. It preserves the earlier observations and artifact identities.

## Why the Completion Conclusion Is Superseded

The phase blueprint requires its authoritative baseline remote, branch, and commit to match the identity in the master plan before capture begins. The master plan and Phase 000 blueprint name `f6ceb9b49fda8b3cf764706bb1e5113b75383ebb`, while the immutable goal names `envy/polish-3.0.4-plan` at `5b3077764907249b3711886cca538794f6139acf` as the applicable checkout. Both identities are real consecutive plan commits, but they cannot both be the required baseline for the same capture. The original packet recorded this difference as reconciled. That is not sufficient for the Phase 000 entry criterion.

The plan must name one authoritative baseline and state whether the existing captured evidence remains admissible before Phase 000 can be integrated.

## Evidence Corrections

| Record | Earlier conclusion | Superseding finding | Current classification |
| --- | --- | --- | --- |
| `#11` Easy Builder enchantment controls | `stale_with_evidence` from a matching JavaScript bundle and a development-server browser workflow | The plan requires the installed JAR browser workflow twice. The browser fixture used matching packed `app.js`, but its Java runtime and CSS differed from the installed JAR. It cannot establish the required installed-artifact result. | `blocked_by_missing_evidence` |
| `#25` visual recipe output lock | `reproduced` from a generic rule emitted after selecting an exact recipe identifier | The observed generic serializer is retained as a real defect observation. However, the selected exact recipe identifier exercises the `locked_ids` intent, not the required recipe-output item-selector intent for `[recipes].locked_items`. The required output-item visual selection, canonical persisted field, compiler and reload activation, and denied and eligible crafting proof have not been captured. | `blocked_by_missing_evidence` for the required output-item workflow |

The malformed `[recipes].locked` rejection result remains valid evidence only for the parse-failure preservation path. It does not establish the unambiguous-migration, compiler-state, reload, or runtime portions of the Phase 000 contract.

## Phase Gate Result

`CORE-PHASE-000` is not ready for integration. The next permissible action is a plan reconciliation that resolves the baseline identity and restores a coherent Phase 000 evidence contract. After that reconciliation, capture the exact installed-JAR browser workflows at the required fidelity before considering a phase transition.
