# ProgressiveStages 3.0.5 acceptance record

This record tracks the 3.0.5 candidate for Minecraft 1.21.1. The current build targets
NeoForge 21.1.248 on both runtime hosts. Earlier 21.1.219 results below retain their historical
scope and do not establish acceptance on the new loader. The endpoint is a checked merge into
`master`, with signed phase tags and no platform release.

The [LuckPerms adapter source audit](luckperms-bridge.md#adapter-source-audit) identifies unresolved
persistence, context isolation, ownership, mutation acknowledgement and cleanup defects in the
audited adapter. The subsequent outbound repair is recorded below and in the same verification
document. Context isolation and complete lifecycle gates remain separate from the selected
provider's dependency only login failure. In memory fixtures and successful builds do not close
BIN-REQ-012 or combined acceptance.

## Outbound node verification

The [outbound node regression](luckperms-bridge.md#outbound-node-regression) records the current
transient storage, reference tracking, mutation outcome and cleanup repair. Its Java build passed
335 tests across 96 suites, including fifteen new isolated API, tracker and shutdown tests.
Real provider context isolation, negative precedence, lifecycle and login acceptance remain open.

Source commit `23d5733fc2b5a2d6b5018740f7af19e13e2d4c75` produced the clean packaged JAR with
SHA256 `9148c753e669740865fead68abda8c5e9e81ed9eedd37ea28a8c90148c994e37`. All 726 project class
entries matched compiled output and no LuckPerms API classes were packaged. A production dedicated
server without optional mods reached readiness, executed the console time query and permissions
capture status, and stopped normally. The linked record contains exact runtime, timing and
cleanup evidence. This bounded check does not close real provider or laptop acceptance.

## Online source transaction verification

The [online source transaction record](luckperms-bridge.md#online-source-transaction-verification)
binds `58f2b43a16a83d34eeca9abddd52ef3fd5ef40a0` to 401 passing unit tests, twelve final dedicated
GameTest invocations and clean packaged startup on NeoForge 21.1.248. A subject's source and episode
changes now commit before callbacks, preserving later administrative mutations. Owner movement has
one notification covering all old and new owners. The record includes the corrected notification
regression, verified cleanup and the remaining provider and client acceptance boundaries.

## Online input collection verification

The [online input collection record](luckperms-bridge.md#online-input-collection-verification)
binds source commit `2f858ee18191700f8eceb6a60ac73aafee5d9f70` to 400 passing unit tests, eight
verified dedicated GameTest invocations and clean packaged startup on NeoForge 21.1.248. Stale
or incomplete query batches cannot partially grant or withdraw input sources. The record includes
verified cleanup and explicitly leaves application transactions and real provider and client gates open.

## Membership generation verification

The [membership context regression](progression-ownership.md#membership-context-regression) binds
source commit `6eabe409d53b7ccfd1b50640fedb93232da6123b` to 399 passing unit tests, ten dedicated
GameTest invocations and a packaged production smoke on NeoForge 21.1.248. It protects public
mutations and delayed offline observations when membership changes but resolved owners remain
the same. The full provider, membership lifecycle and laptop gates remain open.

## Administrative eligibility verification

Commit `77fa1ac6500c7fd849c2acf1990941900ca8c63d` extends revoke suppression and independent grant
handling to public APIs, single and grouped commands, and script bulk bindings. The
[administrative regression](luckperms-bridge.md#administrative-eligibility-regression) records 399
passing unit tests, nine dedicated GameTest invocations and a clean packaged production smoke on
NeoForge 21.1.248. Its JAR SHA256 is
`fe8b52d2fe605cc49bdcc2eeae21bae5db3db2edc4cd9618a34c44736ce5af99`.
This closes the bounded entry point regression, while the full provider and client matrix remains open.

## Permission episode verification

Commits `34f763b3cf7171e23dc1c0fe2f17f034e678b977` and
`1b5c90760f731a5315fa0eab4a77ecafe93b8499` add durable eligibility history, manual suppression,
fixed expiry, offline rearm guards and unreadable attachment preservation. Independently earning
a previously derived stage uses the normal acquisition path once. The [episode regression](luckperms-bridge.md#permission-episode-regression)
records 399 passing unit tests, eleven passing dedicated GameTest invocations and the packaged
production smoke on NeoForge 21.1.248. The final clean JAR SHA256 is
`59d5af457041c6100bcc2dcd413eea292f39f48a25e0ee403465dff97c70cc50`, with 761 matching project classes.
Complete real provider, ownership revision, administrative/reset, row edit and client lifecycle
acceptance remains open. This is current phase evidence, without a merge or release claim.

## Owner clock verification

Commit `5d9034528b799cf80a76a8a392f8504e69b9b46e` separates personal, team and server grant clocks,
updates expiry, slot age and both held duration readers, and prevents an unreadable existing clock
save from becoming an empty replacement. The [clock regression](progression-ownership.md#owner-clock-regression)
records 390 passing unit tests, four passing dedicated GameTests and a packaged production smoke
on NeoForge 21.1.248. The clean JAR SHA256 is
`6d08a10839092ffd4ed519ba7b84cc0baf253421e3207a330f1bf5fa836cc4f6`, with 753 matching project classes. This fixes an
ownership prerequisite. LuckPerms eligibility episodes, manual suppression and full provider and
client lifecycle acceptance remain open.

## Offline contributor verification

Commit `37b597704e49e91e80ff0bdbe4606a4b8c916c5f` adds bounded asynchronous offline observations,
static context queries, current definition and owner checks, retryable owned user cleanup and
persisted contributor scans. The [offline regression](luckperms-bridge.md#offline-contributor-regression)
records 386 passing unit tests, five passing dedicated GameTests including a 300 contributor scan,
and the production dedicated smoke on NeoForge 21.1.248. The clean JAR SHA256 is
`25f286ac6ce06792f368ae8444523fb45d2c6dfd8a2e627d56014f301558cb8d`, with 752 matching project classes. Independent
and permanent source preservation are covered by core fixtures. Real provider login, FTB membership
revisions, source episodes, native permission caches and combined client acceptance remain open.
This is current phase correction evidence, with no merge or release claimed.

## Provider event verification

Commit `55eae9800a0062611929dee4acd006a08d8d1850` connects provider changes to the bounded
subject queue and disables callbacks before retryable listener cleanup. The
[provider event regression](luckperms-bridge.md#provider-event-regression) records 378 passing
unit tests, exact API boundary coverage, the 300 subject and 1,200 callback server fixture,
projection and permission query regressions, and a production dedicated smoke check on
NeoForge 21.1.248. The clean JAR SHA256 is
`d5486500d0fab140e6ec462acdcba8d8f284a2ee5891f56ef019e14bf741cdfe`.
All 741 production classes match the compiled source. Actual provider events, native cache
behavior, offline recovery and the selected provider login gate remain open.

## Permission projection context verification

Commit `b75c04e55364b0da477fb21165e46ab6bbf02807` registers the reserved context calculator
required by outbound nodes. It activates only confirmed current projections and invalidates them
before dirty processing, reload, disconnect and shutdown. Failed calculator teardown preserves the
adapter for retry. Reload now uses the bounded online reconciliation scan. The
[projection regression](luckperms-bridge.md#projection-context-regression) records 374 passing unit
tests, three dedicated GameTests including the 300 subject reload budget, and a production
dedicated smoke check on NeoForge 21.1.248. The clean artifact SHA256 is
`87addfcbde61ac5e0a8fabb0e2d1d4f00cadbe5a60aaf256ef320cf76f807f83`, with 740 matching production
classes and a clean source commit manifest. Actual provider registration, cache and event behavior,
login, offline convergence, native command use and combined gameplay acceptance remain open.
This is current phase correction evidence; no phase integration or release is claimed.

## Saved permission source verification

Commit `33373f85f233351c6181841b5b7bbbdad457bedd` keeps loaded synchronized sources inactive
until qualified revalidation while preserving independent and permanent access. Stored pending
entitlements remain explicitly revocable and prevent repeated first join starter grants. The
[saved source regression](luckperms-bridge.md#saved-source-activation-regression) records 368
passing unit tests, actual load, starter and existing source GameTests, and a production dedicated
smoke check on NeoForge 21.1.248. The clean JAR SHA256 is
`2c58f9ae3974dcfbd409e4b327399fc8c8ca679281850460c8f07df4a7e50413`, with 738 matching production
classes and a clean commit bound manifest. This P003-TASK-005 correction proves core loading and
reactivation behavior. Real provider restart and offline convergence, native script and joined
client behavior, expiry and complete lifecycle acceptance remain open. DEC-006, the immutable
goal, the active phase cursor and the merge only endpoint remain unchanged.

## Obsolete permission owner verification

Commit `f476cbfdd85ee7f8ffcf5f61f76b1fbde8ba150d` withdraws the current subject's synchronized
contributions from obsolete owners and deleted definitions before new inbound grants. Its subject
lookup follows existing persistence, copies and all source removal paths. The
[owner regression](luckperms-bridge.md#obsolete-owner-regression) records 364 passing unit tests,
the actual owner and source GameTests, and a production server smoke on NeoForge 21.1.248. The
clean artifact SHA256 is `be8004e9148edb562f236280bdd9b085bba783770157bc25f6db186723b612c3`,
with 737 matching production classes and a clean commit-bound manifest. Actual FTB event timing,
joined client synchronization, offline recovery, legacy attribution and complete provider lifecycle
acceptance remain open. This is a P003-TASK-005 correction; the goal, cursor and merge only endpoint
remain unchanged.

## Inbound source verification

Commit `dd7162b0c7628bce75273ac2fd0eb46be8c1baa6` repairs conflation of independent grants and
permission sources, and separates player contributions sharing one owner and mapping. It preserves
legacy independent stages and removes deleted synchronized rows for the current owner. The
[inbound regression evidence](luckperms-bridge.md#inbound-source-regression) records 361 passing
unit tests, the actual source and query GameTests, and production startup on NeoForge 21.1.248.
The clean artifact SHA256 is `d00b491aa20165de0d398a9c0191e0959232e68f7ebce9d51b308b18202bba24`;
its manifest identifies the commit and all 736 production classes match. This is a bounded
BIN-REQ-011 and BIN-REQ-012 repair under P003-TASK-005, not complete lifecycle or provider proof.
Legacy attribution, membership and offline cleanup, expiry/suppression, effects, real provider
login and the full combined acceptance remain open. The signed source commit is pushed to the
current phase branch. The saved goal, phase cursor and merge only endpoint remain unchanged.

## Online reconciliation queue verification

The [queue regression](luckperms-bridge.md#online-reconciliation-queue-regression) is bound to
source commit `f5bceb64c21e8a4294129282b7ccfd56cddb6a04`. Online overflow now retains pending
subjects and resumes a fair population scan within the existing sixteen subject tick budget.
Java verification passed 354 tests across 99 suites. The NeoForge 21.1.248 dedicated GameTest
reconciled 300 synthetic subjects through the actual bridge tick method with a controlled adapter;
the existing permission withdrawal fixture passed afterward. The clean JAR SHA256 is
`aa9621c30ddcb187b1066cb0c059501cca8f6e7c5eb9e3a88730a02db0ba4379`, with all 734 project classes
matching compiled output and a clean source commit manifest. This is partial BIN-AC-012F
evidence. Offline contributors, real provider events and latency, invalidation and full lifecycle
acceptance remain open, alongside the documented provider login prerequisite.
The same clean JAR passed a production dedicated startup and console smoke check without optional
mods. Both owned servers exited and the runtime and temporary metadata were removed. Cleanup
restored the exact prior path inventories after removing 967 added build paths; shared libraries,
preexisting runtimes, source and the requested artifact were preserved. The linked record contains
the exact commands, observation times, evidence limits and cleanup receipt.

## Independent permission query verification

The [query regression](luckperms-bridge.md#independent-permission-query-regression) is bound to
source commit `5280889e4ee0cb4270d77a7be5a96206a72b0a34`. The Java suite passed 346 tests across
98 suites and the focused core server GameTest passed twice after correcting the disposable
template namespace filter. Queries retain authoritative context values, preserve independently
sourced membership and distinguish unreadable data from authoritative permission results.
Independent FALSE or unavailable permission data withdraws positive output and prevents new output.

The clean JAR SHA256 is `14ef6b65353e9e17938aa09fd66dda06ab74cd1f4d136311ac008fb38cf4f9d2`.
All 731 project classes match compiled output and no LuckPerms API classes are bundled. The
packaged candidate passed dedicated startup without optional mods on NeoForge 21.1.248 and
executed the console time query and permissions capture status. The linked record contains
runtime details and the distinction between controlled adapter evidence and real provider proof.
Actual provider login, inherited exclusion, negative precedence, marker lifecycle and the complete
lifecycle matrix remain open. This increment does not complete combined acceptance or authorize
final integration.

## Historical automated evidence

The editor package is built from `editor-ui` with the pinned Vite, React, and TypeScript versions.
The Access tab covers ownership inheritance, personal and team overrides, server scope, LuckPerms
inbound and outbound rows, synchronized and permanent retention, context maps, command descendants,
and all four direct interaction types. Pure helper tests cover source preserving ownership,
LuckPerms, command, and Selling Bin interaction roundtrips. `npm run check`, `npm test -- --run`,
and `npm run build` pass. The generated `app.js` is the packaged asset served by the authenticated
loopback editor.

The Java suite passes with `./gradlew test build --no-configuration-cache --rerun-tasks`. The
packaged asset checks pass, and the only product version change is `mod_version = 3.0.5` with the
editor package version set to `3.0.5`. Platform pins remain Minecraft 1.21.1, NeoForge 21.1.219,
Java 21, Gradle 8.8, and ModDev 2.0.140.

## provider artifacts

The selected optional provider stack is LuckPerms NeoForge 5.4.140 with the compile only API 5.4
surface and NeoEssentials 1.0.4 build 61 for the command side effect fixture.

| artifact | sha 256 |
| --- | --- |
| ProgressiveStages 3.0.5 candidate jar | `6e8a5440434743dcb3b7dd5b3ea96885c9d2fef4b01964caf2b35c3617630cff` |
| LuckPerms API 5.4 | `086e3971ea63c0b5ad567881b2dfd955acbbffa24fe85ceac8abf54b114ce986` |
| LuckPerms NeoForge 5.4.140 | `6b8097a7e1a27d870d3d472d00079fa958271db16b9433369dce6c7f62530b19` |
| NeoEssentials 1.0.4 build 61 | `60557f5942985fc538b10d0bd93c8f0065f964e1ee1f38649e3161713e74a506` |

## runtime boundary

The exact LuckPerms and NeoEssentials candidates loaded on a disposable no GUI dedicated server
with Minecraft 1.21.1 and NeoForge 21.1.219. LuckPerms enabled successfully and the server
reached `Done`. A second run without the LuckPerms jar also reached `Done`, with the bridge dormant.
The runtime was stopped and its owned directory was removed after log inspection.

The Brave browser journey, provider managed rank changes, real NeoEssentials command side effects,
and the silent laptop client matrix remain unverified until the required Brave connection and
laptop desktop session are available. No browser or client claim is made from the headless checks.

## Selective insertion builder followup

The September 12, 2026 followup adds **Also restrict GUI insertion** to a new held item rule.
The option writes the matching block inventory rule in the same draft mutation. It uses the
same item and block selectors, a lock effect, and an editable inventory priority initially set
to 100. Each resulting rule remains independently editable. The original armor and wildcard
presets remain standalone unless the author explicitly selects the pairing option.

On the Phase 003 branch after `b70d9c4f29d5ed4a9316101d2dfe2c2b02a6f3ba`, the following
bounded headless checks passed on `node-1`, with the pinned platform and frontend dependencies.

| Check | Result |
| --- | --- |
| `npm --prefix editor-ui run check` | TypeScript passed. |
| `npm --prefix editor-ui test -- --no-cache` | 27 tests across seven files passed. |
| React form coverage | Bread pairing produces one draft mutation with both rules, preserves an existing conditional rule, and adds neither a wildcard nor a whole block access rule. The standalone wildcard preset and cancellation also pass. |
| `npm --prefix editor-ui run build` | Packaged editor rebuilt through Vite. |
| `./gradlew test build --no-configuration-cache --console=plain` | 246 Java tests across 87 suites passed, with no failures, errors, or skipped tests. |
| Packaged asset comparison | `app.js`, `app.css`, and `index.html` are byte identical to their source resource files in the candidate JAR. |

The updated JAR SHA256 is `118ae4d7258779e309c219322f975381cdce1c0451884dbfe88a1471149ad56b`.
Its `app.js` SHA256 is `d4ab132d21ae0a108334716d7c1c39c11ff729952ebd81795a3a95bc95ab6148`.
The earlier candidate hash above remains historical evidence for its own runtime only.

These form tests use jsdom and mocked draft storage. They do not prove a Brave workflow,
server validation and apply, an undo or export roundtrip, or actual Selling Bin behavior.
Those acceptance gates remain open. The server correction and its separate real dispatch
GameTest evidence are recorded in [the interaction repair record](selling-bin-interaction-repair.md).

The five documentation checks passed after these guide and evidence edits. All commands exited.
The owned frontend dependency installation, bounded scratch directory, and new verification
build output were removed after inspection. Preexisting build entries, the current candidate,
source assets, shared caches, and the existing bin investigation runtime were preserved. This
suite launched no server, browser, or laptop client and created no laptop resources.

## NeoForge 21.1.248 verification

The loader update changes only `neo_version` from 21.1.219 to 21.1.248 on the Phase 003
branch based on `a031029d1f991ebbd949f430af76bd7e7cd539cf`. Minecraft remains 1.21.1,
Java 21, Gradle 8.8, ModDev 2.0.140 and the existing Parchment and optional dependency pins.
The changed `gradle.properties` SHA256 is
`06ab39f9a24f46753e54bc3f65a5104fa1e4abf6fcdcbff55c5396152233dd94`.

On September 12, 2026, `node-1` ran the following bounded checks with OpenJDK 21.0.11.
The build and server task graphs were inspected before launch. No client or renderer ran.

| Check | Result |
| --- | --- |
| `./gradlew test build --no-configuration-cache --no-daemon --console=plain` | Passed with 246 tests across 87 suites and zero failures, errors or skips. |
| Packaged editor assets | `app.js`, `app.css` and `index.html` match the source resource bytes. |
| Dedicated core startup | The development server launched `forgeserverdev`, Minecraft 1.21.1, NeoForge 21.1.248 and `--nogui`, bound only to loopback, and reached readiness without optional providers. |
| Real block use regression | The cauldron test passed denial with authoritative item preservation and inventory correction, successful stage grant and bucket exchange, then offhand denial after revoke. |
| Inventory insertion regressions | All 18 existing cases passed individually, including temporary rules, swaps, partial stacks, quick movement, dragging, extraction, player and block targets, armor, separate players, current rule snapshots and playerless hoppers. |

The GameTests used the owned `build/loader248-verification` runtime with EULA readback before
launch, a temporary init script selecting that runtime, standard input forwarding and both
`progressivestages` and `minecraft` namespaces. Each case ran through the registered `test run`
command. The fixture cleared the previous test region and confirmed fresh lime success glass
through an authoritative server command before accepting the result. The initial flat world
configuration omitted its layer settings, logged a configuration error and used its fallback;
the fixture was corrected and the subsequent startup reached readiness without that error.

The compiled mod JAR remains byte identical to the preceding candidate,
SHA256 `118ae4d7258779e309c219322f975381cdce1c0451884dbfe88a1471149ad56b`.
A matching JAR hash alone does not identify the loader. The resolved classpath, launch arguments
and configuration above bind these new results to 21.1.248. The matching patched source archive
SHA256 is `52fccc4a4f8a94c7f9ea13330002bc858f6cb85d2ccf1b9496146d4e0c5f9b18`;
its server `useItemOn` returns a canceled event result before block dispatch. The actual cauldron
regression provides server behavior evidence beyond that source inspection.

These checks do not prove the Selling Bin artifact startup or selective sales, LuckPerms login
and provider lifecycle, the Brave authoring journey, or laptop prediction and reconnect behavior
on 21.1.248. Those final matrix gates remain open, and the current followups remain unmerged.

The four documentation reference tests passed after the guide and evidence changes. Both owned
dedicated server runs exited through `stop`; their launch commands finished successfully and
no process remained in the runtime directory. Cleanup removed the disposable server and
666 new build entries plus 0 new local Gradle entries. The 837 preexisting
build entries, 26 preexisting local Gradle entries, candidate JAR, shared dependency caches
and preexisting `run-248` investigation directory were preserved. No laptop or browser
resource was created.

## Actual bin server transaction evidence

The [exact bin transaction record](selling-bin-interaction-repair.md#actual-selling-bin-transactions-on-neoforge-211248)
adds eight passing server GameTests for the actual Selling Bin 1.6 artifact on NeoForge 21.1.248,
with a repeated partial automatic case. The controlled price pack proves armor and wildcard
denial, paired bread GUI denial with an allowed carrot control, both physical parts, stage grant
and revoke, prior accepted deposits and bounded stack merges with manual and automatic sales.
The [test guide](../test/selling-bin.md) gives exact opt in setup, commands and coverage limits.
The [dependency manifest](selling-bin-artifacts.json) preserves the unresolved library metadata
and license observations. This closes only those server route checks. Actual client input,
prediction, GUI presentation, reconnect, the remaining bin matrix, LuckPerms and Brave acceptance
remain open. The updated test candidate JAR SHA256 is
`fbb78556fa0b3193f33b290a1f26e60c88fdb2cd60bd2016f8e745dfefd1487f`.

## Client interaction prediction followup

The current followup adds synchronized direct interaction prediction to the existing compiled
snapshot transport while preserving protocol 2. Matching clients stop denied local block and
item prediction, and the normal server request remains authoritative. Selector pairs and the
server enforcement flag activate atomically. Existing effective stage and creative bypass
updates control the client decision. Empty hands, unrelated items and independent whole block
access retain their separate semantics.

The Java 21 build on NeoForge 21.1.248 passes 253 tests across 88 suites. The seven new tests
cover tag and wildcard selection, selective bread, overlapping stages, grant and revoke,
disabled enforcement, snapshot replacement, legacy capability handling, disconnect clearing
and malformed payload rejection. See the [interaction repair record](selling-bin-interaction-repair.md)
for exact source and artifact hashes, server regression evidence and remaining physical client
acceptance. This followup does not close the Brave, provider, laptop or final merge gates.

## Joined client and selective sale evidence

The [joined laptop verification](selling-bin-interaction-repair.md#joined-laptop-verification)
records actual input on the 21.1.248 candidate at commit
`2874f2756fb4d996433212ef8196362765dc1e9b`. Both original direct rules denied mutation and
allowed real sale after grant. Selective bread rules blocked direct and GUI insertion while
carrots remained sellable and an empty-hand click could open the menu. Placement, dragging,
shift click, hotbar and offhand swap checks preserved denied items. Manual and automatic sale,
revocation in an open menu, reconnect and a separately configured whole bin access lock were
exercised against server counts and rendered state. The resource reload test also verified
mute on the replacement client audio stream before input resumed.

These results cover the listed bin scenarios only. Full ownership and LuckPerms lifecycle
combinations, Brave authoring, the remaining bin matrix and final default merge verification
remain required. The linked record and its bounded observations retain exact artifacts,
selected images, cleanup evidence and the unresolved dependency limitations.

## Selected LuckPerms login gate

The [dependency only login check](luckperms-bridge.md#neoforge-211248-dependency-only-login-failure)
on NeoForge 21.1.248 reached server readiness with the exact LuckPerms 5.4.140 candidate, but
the actual muted laptop client was disconnected with `Invalid player data`. The failure occurs
in LuckPerms capability initialization with ProgressiveStages absent. Provider dependent final
acceptance remains open. Both hosts are cleaned up, and the isolated laptop instance has
its original files and path inventory restored.

The [command execution regression](luckperms-bridge.md#command-execution-regression) passes on
21.1.248 with 259 unit tests, a dedicated server GameTest covering direct and redirected stage
checks and custom execution callbacks, and packaged server startup. Its test resources are cleaned
up. Real provider, joined player delegation and the complete final acceptance matrix remain open.

## Diagnostic capture timing and output regression

Source commit `14f73fa5eb8b6eee3aff4cb6c3c77ac56631f13c` fixes capture clock mixing, cross category interaction records,
stale writer status, replacement before output closes, JSON control character escaping, and
collection bounds. Permission and progression capture commands now register beneath
`/stage debug`, as documented. The complete candidate JAR SHA256 is
`66beaf34364011fe8acb47128b2d44abc99be99655ea9437ab8af3f6785e93bb`.

Verification on node-1 used Java 21.0.11, Minecraft 1.21.1, NeoForge 21.1.248, and the
existing wrapper. `./gradlew test build --no-daemon --no-configuration-cache --console=plain`
passed 270 tests across 89 suites, with no failures, errors, or skips.
No formatter task is configured; compiler checks and `git diff --check` passed.
The eleven new unit tests cover target/category isolation, stopped and expired formatting,
rate/sample/UTF-8 limits, FIFO drain, replacement readiness, a late output collision that preserves
the existing file, control characters, bounded stage arrays and capture local owner labels.
The 200 sample limit bounds the queue below its 256 capacity; this is not a queue overflow test.

The dedicated development GameTest `captureusesserverticksinanexistingworld` passed on the
final source. It checks all three capture command trees and rejection of a permission level zero
source, rejects an unconnected player as a capture target, records while persisted world time is
100000 ticks ahead, restores world time, and requires the writer to close with no queued records.
Its structure metadata at `0 101 3` identified the exact test, and the server condition checking
`minecraft:lime_stained_glass` at `-1 100 2` emitted `diagnostic_capture_final_pass`.
The earlier clock/writer fixture also passed twice before the command registration assertion was
added. Every accepted runtime record parsed as JSON and used the server tick origin.

An initial structure at the minimum world height could not provide a usable result marker.
A subsequent vanilla `test locate` scan loaded a large area and triggered the watchdog in
`StructureUtils.findStructureBlocks`; that failed inspection is not a passing runtime result.
The replacement run used a loaded chunk, a fixed height reference, and an exact marker predicate.
It exited normally. This evidence does not claim a repair to Minecraft's broad locator command.

The final packaged server reached readiness with no optional providers installed. Console calls
to `/stage debug interactions status`, `/stage debug progression status`, and
`/stage debug permissions status` each returned structured category, limits and writer state.
Each category accepted `off` idempotently and rejected `on missing_fixture` with
`No player was found`. The packaged server then saved and exited successfully.
These are console and core server checks, not a positive joined player capture workflow.

The suite reused the owned `build/diagnostic-capture-verification` runtime inside the phase 003
worktree, with loopback binding and its existing dependency library symlink. No client was started.
All owned dedicated processes and single use build daemons exited. The owned runtime, world,
logs, crash report, copied JAR, and newly created verification output were removed after the final
consumer. The pretest build path inventory and dependency libraries were preserved; no added
paths remained in the inventoried build/cache directories. Metadata scratch was removed after
this record was retained. The existing candidate artifact remains available for subsequent gates.

BIN-REQ-005 and BIN-REQ-014 remain partially verified. Complete category and editor diagnostics,
candidate/configuration fingerprint records, the required runtime overhead measurement,
positive online capture lifecycle, actual provider and client acceptance, Brave authoring,
and the final combined default branch gate remain open. The saved goal and phase cursor are unchanged.

## Reviewed draft revision regression

Source `8efa7a816a7d74e4621c750392d23368095adb23` binds the authenticated editor apply operation to the revision displayed
in the review. The earlier frontend sent only confirmation, and the session service applied the
current draft without comparing its revision. The repaired service rejects missing or stale
revisions before the apply transaction, holding the draft monitor across the check and apply.
The frontend refreshes after a conflict and requires another review. Existing configuration
conflict and validation checks still run for a current revision.

On node-1 with Java 21.0.11, Minecraft 1.21.1 and NeoForge 21.1.248, `npm run check`,
`npm test` and `npm run build` passed. The frontend suite contains 29 tests across eight files.
The two added editor context tests verify that apply uses the displayed review rather than the latest
bootstrap revision, follows a newly opened review, and discards/refetches a rejected stale review
before another attempt. These tests use the component test environment, not Brave.
`./gradlew test` and `./gradlew build` passed 270 Java tests across 89 suites with no failures,
errors or skips. No formatter task is configured; compiler checks and `git diff --check` passed.

The dedicated development GameTest `sessionapplyrejectsunreviewedchanges` passed twice through
`EditorSessionService.open` and its authenticated JSON request handler. A privileged test actor
opened and reviewed a valid draft, then changed its source. Stale and missing apply revisions
returned `draft_conflict`, preserved live files and the loaded snapshot, and reported the current
revision. Reviewing the changed draft then allowed the normal file transaction and loader reload.
The test removed its draft, applied stage and transaction backup and restored the prior stage set.
Exact structure metadata at `0 181 3` identified this test. The lime marker predicate at
`-1 180 2` emitted `editor_revision_gametest_pass` and `editor_revision_repeat_pass`.
This proves the core authenticated session handler, not the browser, HTTP bridge, network payload,
or an actual joined operator workflow.

The packaged candidate SHA256 is `aa1f2af7bf33c40a162f005bcc7bfcdd0cd748d7b0fd562dcf49ffa421a11d87`.
The packaged `EditorSessionService` and test class matched the runtime class files used above.
All three packaged editor assets matched the rebuilt resources byte for byte. Their SHA256 values are
`app.js`: `2f933bde2724129646d6a1396791f8ece3593bb23e20e06229b2705edf80c012`,
`app.css`: `3967155ef9490c166b9858dfab9914e151cbfaed9bfe379977c6a5bd5891c2c8`, and
`index.html`: `4de7b397406f7a0f397839ac757726d29e162e0758443bbc962204098d57cd05`.
The packaged dedicated server reached readiness, accepted a diagnostic status command, and saved
and exited normally. All version pins remained unchanged. No client or browser was launched.

CodeGraph queries returned the anchor and an older phase rather than the active worktree for
some symbols. Bounded reads of the identified active files supplied the missing evidence.
The Chrome connection tools are absent from the current tool set; no alternate browser was used.
Brave acceptance, the full editor diagnostic category and candidate/configuration capture header,
provider/client combinations, security findings and the final checked merge remain open.

The owned `build/editor-revision-verification` runtime, world, logs, copied JAR, temporary
frontend dependencies and newly created verification output were removed after their final
consumer. Both servers and the single use build daemons exited. No temporary editor validation
directories remained. The before/after build/cache inventory has no added paths, the shared
dependency libraries and existing candidate artifact remain available, and metadata scratch was
removed after preserving this record. The saved goal and phase cursor remain unchanged.


## Diagnostic candidate identity regression

Source `3bda82032a2aec8b1d444e1b4f9b0bc1e0419246` adds a sequence zero capture header containing the loaded
stage revision, a schema 1 effective configuration fingerprint, loaded mod versions and bounded
archive identity. The build records its commit and production input dirty state in the manifest.
The fingerprint covers baked main settings and the immutable compiled stage map separately from
the existing transport checksum. Header hashing runs on the existing writer, shares its byte
budget, and does not expose paths or raw configuration. Main configuration reload also stops capture.

On September 12, 2026, node-1 with Java 21.0.11, Minecraft 1.21.1 and NeoForge 21.1.248 passed
`./gradlew test` and `./gradlew build`, with 278 tests across 90 suites and no failures, errors or
skips. The eight new tests cover map and set ordering, ordered list and condition distinctions,
actual parsed interaction, ownership and permission values, source path independence, cycles and
unsupported values, archive hash and manifest extraction, output ordering and the shared byte
budget, manifest truncation with complete version fingerprinting, and failed writer cleanup.
No formatter task is configured. Compiler checks and `git diff --check` passed.

The dedicated development GameTest `captureusesserverticksinanexistingworld` passed with 50
loaded stage definitions. Besides its existing clock, command authorization and writer checks,
it required a configuration fingerprint and at least the mod, game and loader identities.
Exact structure metadata at `0 181 3` identified the test, and the lime marker predicate at
`-1 180 2` emitted `capture_identity_gametest_pass`. The output contained an 849 byte
header and one decision, totaling 1102 bytes. Its configuration SHA256 was
`00c1bc538d8821a13469d4638972bb24006082a9b4c27bbba0143692f3308344`.
The development mod correctly reported unavailable archive identity. The loader and game entries
reported 21.1.248 and 1.21.1; both resolved to the development merged archive. This is development
runtime evidence, not a production capture with a joined player.

The rebuilt packaged candidate SHA256 is
`fc6c14838e156983dccbe52849626a4f5f6f9210108c9f1e526d0fc4d54bb83c`.
Its manifest records the source commit above and `Build-Dirty: false`. Packaged capture,
fingerprint, manager and GameTest classes matched the class files used by the verified tests.
The production dedicated server reached readiness, loaded 50 definitions, returned permission
capture status, accepted idempotent off and rejected a nonexistent target, then saved and exited
normally. This verifies packaged startup and negative controls. It does not prove positive online
capture or the production runtime archive locator. No client or browser was launched.

The suite reused `build/capture-identity-verification` inside the phase 003 worktree with loopback
binding and the preexisting dependency library symlink. The development server, production server
and single use Gradle daemons exited. The owned runtime, world, logs, copied archive and newly
created verification output were removed after retaining this evidence. The before and after
build/cache inventory contains no added paths. Shared libraries, preexisting paths and the
existing candidate archive were preserved. Metadata scratch was removed after its final consumer.

BIN-REQ-005 and BIN-REQ-014 remain partially verified. Complete editor and permission category
records, measured runtime overhead, positive online capture lifecycle, real provider and client
acceptance, Brave authoring, security finding resolution and the final checked merge remain open.
The immutable goal and phase cursor are unchanged.


## Editor operation capture regression

Source `d39342c4937c81f36a5662fc4b81d3f6eadd6319` adds `/stage debug editor on <player>`, `status` and `off`
to the shared capture manager and observes authenticated editor session operations. One accepted
operation reserves a bounded queue entry before mutation; an apply result can complete that entry
after its reload stops new capture observations. The existing writer hashes immutable before and
after source maps and replaces the byte reservation with actual output size. The shared limit
reason labels now match `sample_limit` and `byte_limit`.

On September 12, 2026, node-1 with Java 21.0.11, Minecraft 1.21.1 and NeoForge 21.1.248 passed
282 Java tests across 91 suites, with no failures, errors or skips. The four new tests verify
completion across reload with rejection of new operations, immutable source snapshots and path
redaction, shared byte reservations, category isolation and failed observation drain. Existing
capture tests continue to cover the shared rate, sample, byte and output failure behavior.
`./gradlew build`, compiler checks and `git diff --check` passed. No formatter task is configured.

The dedicated development GameTest `editorcaptureexplainsrejectedandcommittedapply` passed twice
through the authenticated `EditorSessionService` handler. An invalid server scope plus team
override rejected apply without writing the stage or changing the installed revision. A stale
revision produced a distinct conflict. A subsequent valid personal stage applied and reloaded,
and its already accepted capture entry recorded matching new apply and definition revisions.
A bootstrap after reload produced no new capture entry. Each run wrote one header and five
operation records, totaling 4642 and 4653 bytes respectively. Source text and file paths were absent.
The test cleaned its draft, stage and transaction backup and restored the original 50 definitions.
This fixture supplies a privileged server actor and installs a local capture for the handler test;
it does not prove online player selection, network transport, a joined operator or Brave behavior.

Exact structure metadata at `0 181 3` identified each test. The lime predicate at `-1 180 2`
emitted `editor_capture_gametest_pass` and `editor_capture_repeat_pass`. The updated
`captureusesserverticksinanexistingworld` also passed, with `editor_capture_controls_pass`
confirming all four capture command trees, permission level zero rejection and the existing
clock/header/drain assertions.

Packaged candidate SHA256 `abb3bf18d3b8fce30a1c06cb9dd82a407ac728b742c636ffabfbfce4b09feb3c` records the source commit above
with `Build-Dirty: false`. The packaged session, capture, command and relevant GameTest classes
matched the tested class files. The isolated production server reached readiness, loaded 50 stages,
returned editor capture status, accepted idempotent off and rejected a nonexistent target.
It then saved and exited. No client or browser was launched. Field and row diagnostics, complete
capability and permission observations, runtime overhead, positive online lifecycle, browser and
provider workflows, security closure and final integration remain open.

### Runtime setup correction and preservation

The first development launch incorrectly used the preexisting checkout `run` directory because
ModDev 2.0.140 assigns `RunGameTask.gameDirectory` during execution and overrides JavaExec
`workingDir`. That server used its existing port 25570 configuration and existing `acceptance`
world. It was stopped without executing gameplay tests. Startup rewrote server metadata and the
world was saved. Those preexisting files were preserved; no claim is made that the world remained
byte identical. Rotated logs containing earlier data were also preserved.

A guard then prevented another default directory launch. A subsequent relative path resolved
against the temporary init script directory and its server failed to bind default port 25565,
which was already occupied. That process exited and its newly created temporary runtime was
removed. The unrelated listener was untouched. The corrected launcher uses an absolute
`gameDirectory` and checks that exact property, `eula=true`, and loopback endpoint before launch.
All passing gameplay evidence above comes from `build/editor-capture-verification` with those
guards and loopback port 25589. The failed setup launches are not acceptance results.

The isolated development and packaged servers and build daemons exited. The owned runtime,
world, captures, copied archive, temporary validation output and newly created build/cache paths
were removed after evidence retention. No added paths remain in the inventoried build/cache
directories. The two current logs created by the incorrect default run launch were removed;
rotated earlier logs and the preexisting runtime data described above were preserved. Ports 25589
and 25570 were free, and no process remained in either used runtime. Shared dependencies and the
existing candidate archive were preserved. Metadata scratch was removed after its final consumer.
The goal and phase cursor remain unchanged.

## Access validation diagnostics

Verified September 12, 2026 on node-1, Java 21, Minecraft 1.21.1 and NeoForge 21.1.248.
Source commit `75e8ce08a53c739fe9dcd659f890235f8b4d5351` is signed and pushed to the Phase 003
branch. This bounded change contributes to BIN-REQ-015 and P003-TASK-004. It does not complete
inline browser presentation, capability resolution or diagnostic capture projection.

Malformed bridge booleans and non-string group or permission arrays now reject instead of
silently coercing or dropping values. Parser errors retain the owning field and a valid stable
row ID through legacy parsing, package parsing and the public validation API. Draft validation
adds JSON diagnostics without removing its existing summary fields or five argument constructor.
Unknown package error locations remain summary errors instead of being assigned a guessed path.

The focused parser and editor tests passed. The full build passed with 293 Java tests in 91
suites, no failures, errors or skipped tests. Compiler checks and `git diff --check` passed;
no formatter task is configured. No frontend source or packaged assets changed.

The dedicated GameTest `editorcaptureexplainsrejectedandcommittedapply` passed twice through the
authenticated session handler. Rejected apply returned `stage.team_stage`, `server_override`
and the correct draft relative identity path while preserving live files and definition revision.
The existing stale revision rejection, successful subsequent apply, reload stop and capture drain
assertions also passed. Each run was confirmed by its structure metadata and the lime success
block, with separate `field_diagnostic_apply_pass` and `field_diagnostic_apply_repeat_pass` markers.
This fixture remains a core handler test, not an online player or browser acceptance result.

The commit bound archive has SHA256 `0149c3c9298e7f708c1b7f65bbd1ddb4a16e1d748012d21941e32a104f720ebb`,
`Build-Dirty: false`, and all 714 packaged classes match the tested class output. Its isolated
production server reached readiness, loaded 50 stages and reported all 50 files valid through
`stage validate`, then saved and exited. The development and production runs reused the owned
`build/field-diagnostics-runtime` with exact `gameDirectory`, EULA readback and loopback
`127.0.0.1:25589` guards. Neither used the preexisting `run` or `run-248` world. No laptop client
or browser was launched. All other final phase gates remain open at their previously recorded
scope, including real provider and combined gameplay acceptance, security and integration.

Both owned servers exited. The runtime, world, logs, captures, copied archive and newly created
build and local cache paths were removed after their final evidence consumer. The libraries
symlink was removed without following it. Shared dependencies, the candidate archive, source and
preexisting runtime data were preserved. Temporary ownership and launch metadata were removed.
The saved goal and phase cursor remain unchanged.

## Builder field feedback and capture projection

Verified September 12, 2026 for source commit `e05895d805fa8617a21993ed5eee66874a7a79d0`,
signed and pushed to Phase 003. This implements the client and capture consumers of the previous
field diagnostics work under BIN-REQ-015 and P003-TASK-004. It does not close browser acceptance
or complete capability production.

The builder retains validation for the matching draft ID and revision, hides results after edits,
ignores delayed stale success notifications, and preserves rejected apply diagnostics while
blocking that rejected review. Ownership controls and access rows display their returned errors
and warnings with explicit severity labels. Summary and review retain ordinary errors without
structured locations. Capture output selects one error before warnings, includes the file role,
field, stable row ID, severity, diagnostic code and total count, and reports truncation separately.
The request outcome is retained as `operation_code`. Source paths and messages remain excluded.

Type checking passed. All 34 frontend tests in nine files passed, including delayed validation,
failed apply retention, row and file routing, warning distinction and older summary only responses.
The normal Vite build regenerated the checked in assets. All 294 Java tests in 91 suites passed
without failures, errors or skipped tests. The Gradle build, compiler checks and diff whitespace
check passed. No formatter task is configured.

The dedicated `editorcaptureexplainsrejectedandcommittedapply` GameTest passed twice. Its real
session handler rejected invalid ownership, returned the owning field, retained source and runtime,
rejected stale apply, accepted the corrected draft, stopped on reload and drained its capture.
The captured invalid record contained `stage.team_stage`, `ERROR`, `server_override`,
`validation_failed`, identity file role and one diagnostic. Both six line captures were 5168 bytes.
Metadata plus the lime success block confirmed each run through `editor_field_capture_pass` and
`editor_field_capture_repeat_pass`. This is core handler evidence, not joined player or browser proof.

The final archive SHA256 is `45f390d6080af8f80c402fa9ac07740f295f2efff828c8b8d5351285227ff38e`.
Its manifest identifies the source commit above with `Build-Dirty: false`. All 715 packaged classes
match the tested class output, and all four editor assets match the normal Vite output. The
packaged server reached readiness, loaded 50 stages, reported stopped editor capture status and
accepted idempotent off, then saved and exited. Both runs used node-1, Java 21, Minecraft 1.21.1,
NeoForge 21.1.248 and `build/diagnostic-ui-runtime` at loopback `127.0.0.1:25589`, guarded by exact
`gameDirectory`, properties and EULA readback.

No Chrome or Brave control tool was available in the session. No browser substitute, graphical
client or laptop runtime was launched. Actual rendering, keyboard and responsive behavior remain
unverified. Complete capability resolution, remaining validation coverage, provider and combined
gameplay acceptance, diagnostic lifecycle and overhead, security closure and final integration
remain open. No release was published.

Both owned servers and verification processes exited. The disposable runtime, world, logs,
captures, copied archive, newly installed frontend dependencies and newly created build and local
cache paths were removed after their final consumer. The libraries symlink was removed without
following it. Source, generated assets, candidate archive, shared dependencies and preexisting
runtime data were preserved. Temporary launch and ownership metadata were removed. The saved
goal and active phase cursor remain unchanged.


## Draft provider and command capabilities

Verified September 12, 2026 for source commit `907ac31dfa6074de803d1193a4d79a600ae16b03`,
signed, pushed and verified by GitHub on Phase 003. This implements capability production and
editor feedback under BIN-REQ-015 and P003-TASK-004. NeoForge remains exactly 21.1.248.

Bootstrap retains the old capability list and returns typed draft capabilities and validation.
Shared validation checks successfully parsed draft definitions, including new outbound groups
and command paths. Command resolution uses the live dispatcher independently of LuckPerms.
Missing and ambiguous paths warn without rewriting source. Team feedback distinguishes the
current global setting, personal and server ownership, and unavailable provider fallback.
LuckPerms feedback distinguishes provider states, disabled mappings, and pending or missing groups.
Only a completed negative asynchronous group query confirms absence; requests and cached results
are bounded. The optional provider API is isolated from core startup. Real provider lookup and
login acceptance remain open.

Type checking, all 37 frontend tests in ten files, and the normal Vite packaging passed. All
302 Java tests in 93 suites passed with no failures or errors. New tests cover deduplicated draft
references, command resolution without LuckPerms, ambiguity, provider absence states, unknown
pending results, failure recovery, expiration, request and cache bounds, late completion after
clear, and draft revision feedback. The Gradle build and diff whitespace check passed; no formatter
task is configured. A nullable team mode type error was corrected before final frontend checks.

The dedicated `editorcaptureexplainsrejectedandcommittedapply` GameTest passed, then passed again
after reload. It checks bootstrap compatibility, field addressed rejected apply, new draft group
and command capabilities, warning only apply, exact saved source, stale rejection, reload shutdown
and bounded capture drain. The first run exposed a test expectation of exactly one diagnostic;
the current default pack legitimately adds 50 team fallback warnings. The corrected assertion
requires those baseline warnings plus the one rejected field, preserving every original source
and revision assertion. Success metadata and lime block checks produced `capability_fixture_pass`
and `capability_reload_fixture_pass`. The passing captures contained six lines, 5175 and 5186 bytes,
51 rejected draft diagnostics and 52 valid draft warnings. Both captured provider state as `absent`.
This fixture uses the actual server session handler with a test operator, not a joined player or
browser. It cannot close positive online capture or graphical acceptance.

The commit bound archive has SHA256
`cede1d93826547e7665d631bb679ed23671a62aa13818912d36d5c5977b192c6` and `Build-Dirty: false`.
All 718 packaged classes match the tested output and all four editor assets match the normal
frontend build. Development and production verification use node-1, Java 21, Minecraft 1.21.1,
NeoForge 21.1.248, the owned `build/capability-runtime`, exact `gameDirectory` checks, EULA readback,
and `online-mode=true` on loopback `127.0.0.1:25589`. Preexisting `run` and `run-248` worlds are excluded.

No Chrome or Brave control tool was available, and no browser substitute or graphical client was
launched. Actual browser behavior, complete field combinations and source preservation, remaining
capture fields and overhead, real providers and combined gameplay, security closure and final
integration remain open. GitHub continues to report 33 baseline dependency alerts. No release
was published. The saved goal and active phase cursor remain unchanged.

The packaged server reached readiness, loaded 50 stages, passed `stage validate` for all 50 files,
reported editor capture stopped and accepted idempotent off. It then saved and exited normally.

Both development server processes and the production server exited. Cleanup removed the exact
owned runtime, world, captures, logs, copied archive, newly installed frontend dependencies,
666 newly created build and local cache paths, and temporary launch and ownership metadata.
The libraries symlink was removed without following it. The loopback port is free and no new
validation scratch directory remains. Source, packaged assets, the commit bound candidate,
shared dependencies, preexisting runtime data and unrelated worktrees were preserved.


## Context authoring and shared validation limits

Verified September 12, 2026 for signed source commit `b8db7c7e75bdb2defaec5e7672349779b99e0df6` on Phase 003.
This advances BIN-REQ-015 and P003-TASK-003 through P003-TASK-005. It does not close the full
builder, provider, or combined acceptance matrix.

Inbound and outbound forms use separate context key and value controls. Commas and embedded
newlines remain part of one value. Duplicate keys reject saving without dropping either row;
cancel and failed save preserve source and input. Context serialization quotes keys, retains
invalid values for authoritative validation, reads multiline arrays and string variants, and
preserves comments and unrelated child tables. Changed mapping fields are written inside their
array row. Inline field comments, unchanged known fields and omitted defaults are retained.
Review found and repaired the old root assignment boundary and comment parsing errors.

The shared domain and parser enforce 8 keys, 8 values per key and 256 combinations per mapping.
Both editor schemas publish those bounds. Legacy and package rejection identify the indexed
context field and stable row ID. Programmatic constructors enforce the same limits.

All 55 frontend tests in eleven files, TypeScript checking and Vite packaging passed. All 308
Java tests in 93 suites passed with zero failures, errors or skips. Gradle build and whitespace
checks passed. No formatter task is configured. Tests include both mapping directions, source
preservation, failed save recovery, malformed source, duplicate keys, Unicode and multiline
strings, exact bounds and overflow, package parity and schema hints. One intermediate regression
test exposed an inline comment being read as part of the mapping ID; the corrected parser passed
the complete frontend suite afterward.

The real session handler GameTest `contextapplypreservessourceandrejectsoverflow` passed. A
nine value outbound context rejected apply with its owning field and row. Bootstrap returned the
unchanged invalid draft; disk and installed revision stayed unchanged. The corrected draft applied
with exact source bytes, dotted context keys and unsplit values, and loaded a 256 combination
outbound map. The fixture removed its package and restored 50 baseline stages. Structure metadata
and a conditional lime block check confirmed `context_apply_pass`. This uses a test operator,
not a joined player or browser, and does not prove LuckPerms eligibility or node projection.

The archive SHA256 is `2d73688a4ce99a1e08217b5a9a69988a04d9c11a062ce4d1f119dd78e588d5de` with `Build-Dirty: false` and the
source commit above. All 720 classes matched tested output; all 4 editor assets matched the normal
frontend build. Development and packaged production runs used node-1, Java 21, Minecraft 1.21.1,
NeoForge 21.1.248, owned `build/context-runtime`, EULA readback and loopback `127.0.0.1:25589` with
online authentication. The inspected development task graph launched only the dedicated server.
The packaged server reached readiness, loaded and validated all 50 stages, reported editor capture
stopped, accepted idempotent off and exited normally.

Chrome and Brave control tools remain unavailable. No graphical client or browser substitute was
launched. Actual browser acceptance, remaining source syntax and field combinations, command
default and editing parity, real provider login and behavior, combined gameplay, diagnostic
overhead and lifecycle, touched security closure and final integration remain open. GitHub still
reports 33 baseline dependency alerts. No release was published.

Both owned servers and all verification processes exited. Cleanup removed the exact runtime,
world, logs, copied archive, editor fixture backups, newly installed frontend dependencies,
699 newly created build and local cache paths, and temporary launch and ownership metadata.
The libraries symlink was removed without following it. The loopback port is free. Source,
packaged assets, the commit bound candidate, shared caches, preexisting run directories and
unrelated worktrees were preserved. The immutable goal and active phase cursor hashes are unchanged.


## Command defaults and interaction row editing

Verified September 12, 2026 for signed source commit `30a9fc2dc82e8dcc03ab7f1528931775efd964c2` on Phase 003.
This advances BIN-REQ-015, SHARED-002, SHARED-005, SHARED-006 and P003-TASK-003 through
P003-TASK-005. The complete builder and combined acceptance matrix remain open.

Omitted command `descendants` now means true in the legacy parser, package parser, schema
metadata and guided form. Explicit false excludes child literals. Editing another field keeps
omission intact. Command and interaction edits now replace values inside their selected array
row instead of inserting assignments outside it. Inline comments, unchanged defaults, conditional
activation, unknown child tables and adjacent rows are preserved in the covered syntax. Priority
with TOML digit separators parses correctly. Failed command and interaction saves display the
error while retaining the dialog and input.

Nine new frontend regression cases failed before the implementation repair. All 69 frontend
tests in eleven files, TypeScript checking, Vite packaging and whitespace checks then passed.
All 312 Java tests in 93 suites passed with zero failures, errors or skips. Gradle build passed
again after the final GameTest fixture correction. No formatter task is configured, and no data
provider changed. Both legacy and package tests cover omitted, true and false descendants.

The dedicated GameTest `parsedcommanddefaultsreachactualexecution` passed all three selections
through actual `time` command execution, a vanilla `execute` redirect, grant, revocation and
native permission denial. The first run exposed a fixture mistake: registering the same stage ID
again retains the original definition. The corrected fixture restores the baseline definitions
before installing each selection and asserts its identity. The existing
`executionrechecksstagesaftervanillaredirects` also passed, including command callbacks and console
semantics. Structure metadata and conditional lime block checks confirmed `command_defaults_pass`
and `command_redirects_pass`. Fixtures restored stage definitions and world times; all 50 baseline
stage files validated afterward. These are server fixture players, not joined nonoperator players,
and do not prove real LuckPerms or NeoEssentials behavior.

The archive SHA256 is `76cae176dcb6fe6aad2e32368dc0d8601bfbc154f884e40567806f861d53dc00`, with `Build-Dirty: false` and the source commit above.
All 720 classes matched tested output and all 4 editor assets matched the normal frontend build.
Development and packaged production runs used node-1, Java 21, Minecraft 1.21.1, NeoForge 21.1.248,
owned `build/command-edit-runtime`, EULA readback and loopback `127.0.0.1:25589` with online
authentication. The inspected development task graph launched only the dedicated server. The
packaged server reached readiness, loaded and validated 50 stages, reported editor capture stopped,
accepted idempotent off and exited normally.

Full quoted table paths, remaining source syntax and field combinations, actual Brave workflows,
real provider login and behavior, combined gameplay, diagnostic overhead and lifecycle, touched
security closure and final integration remain open. GitHub still reports 33 baseline dependency
alerts. No browser or graphical client substitute was launched, and no release was published.

Both development servers, the packaged server and every bounded verification process exited.
Cleanup removed the exact runtime, world, logs, copied archive, newly installed frontend
dependencies, 689 newly created build and local cache paths, and temporary launch and ownership
metadata. The libraries symlink was removed without following it. The loopback port is free.
Source, packaged assets, the commit bound candidate, shared caches, preexisting run directories
and unrelated worktrees were preserved. The immutable goal and phase cursor hashes are unchanged.


## Quoted source preservation and stage discovery

Verified September 12, 2026 for signed source commits `1965fa465d52e47d3349c5696fe69278654d3110`
and `0ddafeffa08f08ea67287f8230c6fbbe5f710fc7` on Phase 003. This advances BIN-REQ-015, SHARED-002, SHARED-006 and
P003-TASK-003 through P003-TASK-005. It does not close the complete source or browser matrix.

Access editing now uses table and assignment spans. Header text inside multiline strings,
comments and nested values is skipped. Quoted and escaped path segments resolve correctly;
literal dots inside a quoted key remain distinct from path separators. Edits retain original
line endings, indentation, field comments, nested child tables, adjacent rows, and unrelated
blank lines. No op row replacement preserves every byte. Multiline context values normalize
CRLF for their parsed value while their unchanged source retains the original bytes. Ownership
and retention changes preserve omitted effective defaults. Ambiguous unterminated values reject
mutation; general nested inline values still require further guided editor work.

Eight new source boundary cases failed against the earlier helpers. A golden ownership fixture
also exposed an unnecessary scope insertion. The old ownership test required that insertion,
contrary to SHARED-006 optional presence preservation. Its replacement asserts exact source,
retains the ownership behavior checks, and adds explicit server to team conversion coverage.
All 87 frontend tests in twelve files, TypeScript checking, Vite packaging and whitespace checks
pass. The five shared `editor-ui/src/lib/fixtures/toml-preservation.json` cases cover ownership,
inbound contexts, outbound contexts, command paths and interaction selectors. Python `tomllib`
independently parsed each before and after document and proved that only the declared target
value differs semantically. The interaction dialog test produced the exact expected fixture bytes.

The first packaged runtime loaded only eight of ten config fixtures because discovery required
a literal stage header. Seven Java regressions reproduced quoted, escaped, dotted and inline
stage omission, a false stage header inside multiline text, and a skipped malformed candidate.
Config and datapack discovery now use parsed TOML. Candidate declarations in malformed source
remain available for normal syntax validation, and hidden archives and helper exclusions remain.
All 320 Java tests in 93 suites passed with zero failures, errors or skips. Gradle build passed.
No formatter task is configured and no data provider changed.

The corrected packaged server loaded all ten config fixtures derived from the five before and
after documents, with only unique fixture stage IDs added or substituted. `stage validate`
reported 60 of 60 valid config stages. A disposable format 48 datapack containing the two quoted
ownership fixtures loaded exactly two additional stages; its multiline note containing a fake
stage header was ignored. Startup and actual `reload` both loaded two datapack stages and 62
total definitions. The data format was checked against the pinned server archive metadata.
Editor diagnostic status remained stopped and off was idempotent.

The production launch did not expose the GameTest command, even with its enable properties.
The inspected dedicated development task graph was used for
`contextapplypreservessourceandrejectsoverflow`. It passed rejected draft retention, corrected
apply, exact source preservation, 256 context combinations and fixture cleanup. Structure
metadata and the conditional lime block marker `source_span_apply_pass` confirmed the result.
Removing the ten config fixtures and disabling the owned datapack restored zero datapack stages
and 50 total stages. These are dedicated server and fixture operator checks, not browser or
joined player evidence.

The final archive SHA256 is `7fef152a1afc486516317c1ff3b3935be04f8fcbce2b43118c7743b61e528d11`, with `Build-Dirty: false` and the second source commit above.
All 720 classes and all 4 editor assets match their verified build output. Runs used node-1,
Java 21, Minecraft 1.21.1, NeoForge 21.1.248, owned `build/source-span-runtime`, EULA readback,
loopback `127.0.0.1:25589` and online authentication. No graphical client was launched.
General inline authoring, other remaining source and control combinations, actual Brave journeys,
real provider behavior, combined gameplay, diagnostics, security closure and final integration
remain open. GitHub still reports 33 baseline dependency alerts. No release was published.

Both packaged server processes, the dedicated development server and all bounded verification
processes exited. Cleanup removed the exact runtime, world, config and datapack fixtures, logs,
editor backups, copied archive, newly installed frontend dependencies, 730 newly created build
and local cache paths, and temporary fixture and ownership metadata. The libraries symlink was
removed without following it. The loopback port is free. Source, tracked fixtures, packaged assets,
the commit bound candidate, shared caches, preexisting run directories and unrelated worktrees
were preserved. The immutable goal and phase cursor hashes are unchanged.

### Inline table field authoring

Source revision `2aab093b785b53140249f5503f380692d7a6b6dc` extends the shared
builder field helpers for BIN-REQ-015 and SHARED-006. Ownership and LuckPerms
retention controls now read, update, add, and remove fields within inline tables.
Nested and dotted paths retain their enclosing structure, quoted keys, sibling
values, trailing comments, and original line endings. Removing the first, middle,
last, or only inline field leaves valid separators. Comments inside a replaced
array are retained before the containing assignment. A scalar or array ancestor
is rejected instead of being overwritten to create a nested table.

Verification on the Phase 003 worktree on node-1:

- `npm test` passed 108 tests in 13 files, including a DOM test that exercises the
  ownership and retention controls against inline stage source.
- `npm run check` and `npm run build` passed. The packaged editor contains the new
  field implementation.
- Twelve checked in before and after fixtures in
  `editor-ui/src/lib/fixtures/toml-inline.json` passed byte exact editor assertions.
  Python's independent `tomllib` parser confirmed each output equals the parsed
  input with only the designated path changed or removed.
- `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 ./gradlew build test --rerun --no-daemon`
  passed. All 320 Java tests in 93 suites passed without failures, errors, or skips.
  A subsequent build bound the clean artifact to the signed source revision.
- The candidate `progressivestages-3.0.5.jar` has SHA-256
  `0231a04a7ae1a7fe28d66c674b1611cbe58056e6f4b62457376b83afd95deab9`.
  Its manifest reports the revision above and `Build-Dirty: false`. All four editor
  assets and 720 compiled classes matched their packaged bytes.
- GitHub verified the source commit's SSH signature. The source is pushed to
  `envy/3.0.5-phase-003`. No phase merge, tag, or release is claimed.
- Cleanup removed the newly installed frontend dependencies, temporary fixture
  audit metadata, and 685 newly created build and cache paths. The candidate and
  preexisting runtime directories remain preserved. All owned command handles
  reached terminal state. The immutable goal and active phase cursor hashes are
  unchanged.

This increment does not close the complete builder contract. Guided inline arrays
of interaction and permission mapping tables, remaining source validation behavior,
the packaged Brave journey, and final combined runtime acceptance remain open.
The DOM checks are not browser or gameplay evidence. No Minecraft runtime or browser
was launched for this frontend increment.

### Inline access array authoring

Source revision `da5ad412ef5910399a8ae14d0dbf64ab96abd69e` extends BIN-REQ-015
and SHARED-006 to the Access tab's inline interaction, command permission, inbound,
and outbound mapping arrays. Existing rows retain omitted defaults and unknown
nested values. Additions use the original collection representation, including a
new mapping array inside an inline LuckPerms table. Removals preserve array comments
once. Context controls handle nested maps and dotted context fields. The paired
Selling Bin preset can populate an existing inline interaction array without
appending a conflicting array table declaration.

Verification on node-1, in the existing Phase 003 worktree:

- `npm test` passed 126 tests in 14 files. The dialog tests exercised the paired
  Selling Bin addition and an existing inline inbound context edit. The row tests
  covered all four access collections, omitted settings, nested conditions,
  malformed mixed arrays, empty arrays, trailing commas, and row removal.
- `npm run check`, `npm run build`, and the Java 21 `./gradlew build --no-daemon`
  passed. All 320 Java tests in 93 suites passed without failures, errors, or skips.
- A separate Python `tomllib` check parsed 68 actual helper outputs across compact,
  spaced, commented, and trailing comma arrays with zero, one, or three rows. The
  parsed result matched the input plus only the requested add, remove, clear, or
  command path edit. No op results were byte identical. Two additional independent
  cases verified dotted context replacement and adding a nested mapping array.
- The clean candidate `progressivestages-3.0.5.jar` has SHA-256
  `3f043fc7747aac84cd624533af685915fe88265ce57ec4a8ebf54b060d43c4b8`.
  Its manifest reports the source revision above and `Build-Dirty: false`. All four
  editor assets and 720 compiled classes matched their packaged bytes.
- The signed source commit is pushed to `envy/3.0.5-phase-003`, and GitHub verified
  its SSH signature. No phase integration or release is claimed.
- The temporary frontend dependencies and independent parser scratch were removed
  after their final consumers. Newly created build and cache paths were removed
  against the preflight inventory. The candidate, source, tracked tests, and
  preexisting runtime directories remain preserved. All owned command handles
  terminated. Goal and active phase cursor hashes remain unchanged.

These results close the specific inline Access array implementation gap recorded
in the preceding increment. They do not establish complete guided condition
editing, the packaged Brave journey, provider compatibility, or combined gameplay
acceptance. No Minecraft client, server, or browser was launched for this frontend
increment. The complete final phase and plan wide gates remain open.

### Inventory condition source preservation

Source revision `50ef36d55d4346f1bdcf1e196820ef04bf688bae` repairs an additional
BIN-REQ-015 and SHARED-006 authoring path. The Rules tab previously reconstructed
an inventory insertion row when saving it. It now changes only edited fields,
retaining descriptions, unknown nested values, omitted defaults, and condition
aliases. Quoted child condition tables remain in place during scalar edits. Inline
inventory arrays are available in the Rules tab. Explicit condition removal keeps
unrelated source and comments. Failed saves retain the form, and a concurrent row
edit requires reopening it before saving. Rule removal and reordering use parsed
table boundaries. Changing away from an inventory or classic rule now retains the
new replacement rule instead of only deleting the previous representation.

Verification on node-1 in the existing Phase 003 worktree:

- `npm test` passed 140 tests in 15 files. Twelve Rules tab DOM tests covered no op
  saves, condition targets, priority, explicit condition removal, failed storage,
  stale source rejection, neighboring sections, and category conversion. Shared
  condition tests also cover quoted target aliases and nested fields named `type`.
- `npm run check`, `npm run build`, and Java 21 `./gradlew build --no-daemon` passed.
  All 320 Java tests in 93 suites passed without failures, errors, or skips.
- Independent Python `tomllib` checks validated 54 actual helper outputs. Each of
  `while`, `when`, and `condition` was exercised as an inline row, a field in an
  array table, and a child table. No op, target, count, reset callback, priority,
  and removal operations changed only their designated semantic value. No op
  outputs were byte identical to their inputs.
- The clean candidate `progressivestages-3.0.5.jar` has SHA-256
  `6574d9d75b3c4c97cdecdf965100bbb8ffbef36671348462a4610d03e0cf0d09`.
  Its manifest reports the source revision above and `Build-Dirty: false`. All four
  editor assets and 720 compiled classes matched their packaged bytes.
- The source commit is pushed to `envy/3.0.5-phase-003`, with its SSH signature
  verified by GitHub. No phase merge, tag, or release is claimed.
- Temporary frontend dependencies, independent parser audit data, and newly created
  build and cache paths were removed after their final consumers. The candidate,
  source, regression tests, and preexisting runtime directories remain preserved.
  All owned command handles terminated. The goal and cursor hashes are unchanged.

This evidence covers inventory insertion condition editing, not complete generic
rule authoring or all condition forms. Broader guided preservation, packaged Brave
acceptance, provider compatibility, and the combined gameplay matrix remain open.
No Minecraft runtime or browser was launched for this frontend increment.


## Outbound projection consistency follow-up

The [outbound projection record](luckperms-bridge.md#outbound-projection-consistency-verification)
binds source `d3ef67367eaf5b881576d326797c218722660db9` to 407 passing unit tests, seven
passing dedicated GameTest invocations, clean package parity, production dedicated startup and
verified teardown on NeoForge 21.1.248. It covers interrupted queries, writes and publication,
exact cleanup ownership and preserved independent stages or deliberate revocation. Actual
LuckPerms caches, provider login, joined client and browser acceptance remain open. The source
is signed and pushed on Phase 003; no merge, tag, wiki update or release is claimed.


## Offline source transaction follow-up

The [offline source transaction record](luckperms-bridge.md#offline-source-transaction-verification)
binds `68f8333dd07c6f387a8596efd3443fc8084c11e1` to 407 passing unit tests, twelve passing
server GameTest invocations, clean package parity, production dedicated startup and verified
cleanup on NeoForge 21.1.248. Rejected drafts preserve live sources, history and clocks; valid
changes commit once and preserve separately committed grants. Both new regressions passed twice.
Real provider and joined client acceptance remain open. No default integration or release is claimed.


## Runtime capture overhead

On September 13, 2026, source `b117b29155f8e1ca1e9e818ddabe98c9c414293a` added
`captureoverheadstaysboundedacrosscategories` and the
[reproducible measurement procedure](../test/diagnostics.md). No gameplay recording behavior,
limits, dependencies, or platform pins changed. This supplies runtime overhead evidence for
BIN-REQ-005, BIN-REQ-014, BIN-REQ-015 and P003-TASK-005, without completing their other gates.

The test ran on node-1, Intel Core i9-13900HK, Linux 6.12.63+deb13-amd64,
Debian OpenJDK 21.0.11+10, Minecraft 1.21.1 and NeoForge 21.1.248. The dedicated development
server launched with `forgeserverdev` and `--nogui`, reached readiness at 07:59:33 CDT,
and started the isolated benchmark at 07:59:46. Its final measured round completed at 08:00:46.
At 08:01:08, the success block and exact structure metadata jointly emitted
`capture_performance_development_pass`. The diagnostic status command then reported off,
zero records, zero queued records and an idle writer. The server saved and exited with code 0.

Each category used one discarded warmup followed by three measured rounds. Each round contained
1,000 enabled and 1,000 disabled recording attempts in fifty paired windows. All twenty enabled
records in each window were accepted before the unchanged rate limit stopped the capture.
Every window drained successfully. The fixture parsed its header and twenty records, checked
category, capture ID and every sequence number, and deleted the file before proceeding.
Across warmup and measured work, this verified 20,000 accepted records in 1,000 capture files.
No dropped record or writer error occurred.

The table retains every measured result. CPU and elapsed values are added nanoseconds per
attempt after subtracting the paired disabled recording baseline. Allocations are total bytes
for all 1,000 disabled attempts in that round. Both timing limits were 100,000 nanoseconds.

| Recording path | Round | Added CPU ns per attempt | Added elapsed ns per attempt | Disabled allocated bytes |
|---|---|---|---|---|
| interactions | 1 | 18407 | 18424 | 0 |
| interactions | 2 | 15650 | 15703 | 0 |
| interactions | 3 | 14012 | 14072 | 0 |
| progression | 1 | 37552 | 37585 | 0 |
| progression | 2 | 35911 | 35964 | 0 |
| progression | 3 | 35850 | 35886 | 0 |
| permissions | 1 | 2438 | 2449 | 0 |
| permissions | 2 | 2124 | 2135 | 0 |
| permissions | 3 | 1093 | 1102 | 0 |
| commands | 1 | 5403 | 5409 | 0 |
| commands | 2 | 4393 | 4397 | 0 |
| commands | 3 | 2469 | 2473 | 0 |
| editor | 1 | 270 | 282 | 0 |
| editor | 2 | 216 | 228 | 0 |
| editor | 3 | 172 | 188 | 0 |

The maximum observed CPU overhead was 37,552 ns and elapsed overhead was 37,585 ns per attempt.
Every measured disabled path allocated zero bytes. Capture startup, asynchronous hashing and
writing, output inspection and tick scheduling were outside the timed region. The fixture used
bounded interaction and progression stage lists and a small editor source map, as documented
in the procedure. These results do not establish worst case performance for arbitrary packs,
capture startup cost, actual gameplay latency, browser operations, or authenticated player input.
The temporary UUID lookup entry permitted the public manager's target lookup; it was removed
before the success marker. It does not establish positive online capture lifecycle acceptance.

`JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 ./gradlew --no-daemon test build prepareServerRun --console=plain`
passed in 17 seconds with 415 tests across 107 suites, zero failures, errors or skips. The build
has no configured formatter or static analysis task; `git diff --check` passed. After the signed
source commit, `test build` passed in 4 seconds, rebuilding the source bound JAR while reusing
the unchanged successful test outputs. All 794 packaged project class files matched the tested
compiled class files byte for byte. The final archive identified the source commit above and
`Build-Dirty: false`.

- Candidate SHA256: `07a8f7bd8e3451a9b3463b54b48316ff04f5b24385970bc178adc09300229fb9`.
- Candidate SHA512: `81dab2504b5c19fafa78ac331f41eb0c438d10b755b4b3d23b79ef47d2e210faaed56ef212d8c983931a175da429c9f491b696da2894768f2d25a334f1a4067f`.

NeoForge 21.1.248's inspected `GameTestHooks.isGametestEnabled` rejects production launches,
so no production GameTest result is claimed. The same archive separately started through the
unmodified installed `unix_args.txt` with `forgeserver` and `--nogui`, with optional providers
absent. It reached readiness at 08:03:10, returned game time 2554 and an idle diagnostic status
at 08:03:43, then saved and exited with code 0. Both complete server logs contained no ERROR
or exception. No client or browser was launched.

Both launches reused the exclusively owned `build/capture-performance-runtime` under the phase
003 worktree, bound only `127.0.0.1:25589`, retained online authentication, disabled RCON and query,
and read back `eula=true` before startup. Production reused the preexisting `run-248/libraries`
through one owned symlink. Development PID 1032261 and production PID 1039169 both exited;
process directory checks found no remaining consumer and the port was free. Cleanup removed
1,062 newly created build paths and 13 newly created local Gradle paths, restoring the exact
836 and 26 path baselines. It removed the runtime and unlinked its library reference without
traversing or modifying the shared libraries. The candidate JAR, source, preexisting `run-248`,
shared dependency caches, unrelated worktrees and their contents were preserved.

The seven private scratch files were removed after the measurements were transferred here.
Control authorization, remaining lifecycle and failure paths, actual provider reconciliation,
Brave authoring, combined client gameplay, security closure and final merge remain open.


## Editor collaborator session revocation

A bounded source review on September 13, 2026 found an authorization gap in the changed editor
request path. `NetworkHandler.handleEditorRequest` dispatches on the server thread, but the
session service previously checked only session ownership, the session secret and current
operator permission. Removing a draft collaborator changed the draft access list without
revoking that operator's existing sessions. Ordinary draft mutations checked membership again,
while read, review and apply dispatch could still use the old session. This required an
already invited permission level 3 operator with a valid session. It was a draft access defect,
not evidence that an ordinary nonoperator could acquire server privileges.

Source `8cdb8e08ef3b16cbe5cb09712a29a69e47f06ba2` revokes every session for the removed operator
and draft, and checks current draft membership on every authorized request. Other drafts and
the owner's session are outside that revocation filter. Restoring collaborator membership does
not revive old sessions. The request protocol, configuration schema and permission level remain
unchanged. This repair belongs to BIN-REQ-014, BIN-REQ-015 and P003-TASK-005.

The [authorization regression](../test/editor-authorization.md) first ran against the unfixed
service with the new fixture only. At 08:12:54 CDT, it failed on the first removed session read
with `An unauthorized editor request must be rejected before its action runs.` The fixture's
cleanup removed its draft and sessions. This expected failure was retained as the reproduction;
it is not counted as a passing verification.

After the fix, the dedicated development server reached readiness at 08:14:50. The following
markers required both a lime success block and the exact test's structure metadata:

| Test | Result marker | Observed CDT |
|---|---|---|
| Removed collaborator actions | `editor_authorization_first_pass` | 08:15:31 |
| Owner review and apply, stale revision rejection | `editor_owner_apply_pass` | 08:15:53 |
| Removed collaborator actions after fixture cleanup | `editor_authorization_repeat_pass` | 08:16:15 |

Each authorization run rejected twenty actions for each of two removed sessions before dispatch,
preserved the live configuration revision and absence of the fixture file, and preserved the
owner's session. Readding the collaborator left both old sessions invalid but allowed a fresh
resume. A different operator and a wrong credential both failed without revoking the legitimate
session. Permission loss revoked the fresh session on its next request; restoring permission did
not revive it. The owner apply regression performed a real validated file transaction and reload,
then restored the fifty definition baseline. Before shutdown, no authorization or revision fixture
stage directory and no editor draft JSON file remained.

The test used server player objects with controlled permission levels. These results establish
the actual session service boundary and file mutation behavior, not authenticated login,
Brave error presentation, client transport or a complete security audit. Session secrets stayed
in process memory and were not included in assertion messages, logs or retained evidence.
Source discovery returned historical checkout matches, so the omitted active service, draft,
apply and network boundaries were inspected directly in the current worktree.

The fixed source passed `JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 ./gradlew --no-daemon test build --console=plain`
in 15 seconds. All 415 tests across 107 suites passed with zero failures, errors or skips.
No formatter or static analysis task is configured. Diff whitespace and the new guide's relative
link checks passed. After the signed source commit, the same build command passed in 3 seconds,
reusing unchanged successful test outputs and rebuilding the source bound archive. All 796
packaged project classes matched the tested compiled bytes. The archive reported the source
commit above and `Build-Dirty: false`.

- Candidate SHA256: `8f586a6ebce9f6bbef51c59451909aaf8fa206e1f62800d1d109c40f5ba1ec13`.
- Candidate SHA512: `44b33a826a622253fe0ae7a681e77908b4d5e88bc3dbdd5bdcf146433923c587e3d168803399dae704c708e0d0463b02d8f402739ccea03aaf2c06aa5ccccdd1`.

The packaged archive separately started with the installed `unix_args.txt`, `forgeserver` and
`--nogui`, with optional providers absent. It reached readiness at 08:17:39 and returned game
time 3769 and an idle diagnostic status at 08:18:15. It saved and exited with code 0 at 08:18:16.
The fixed development and production logs contained no ERROR or exception. NeoForge's production
GameTest restriction remains in effect; no production GameTest run is claimed.

All three launches used node-1, Debian OpenJDK 21.0.11+10, Minecraft 1.21.1 and NeoForge 21.1.248,
with the owned `build/editor-authorization-runtime` under the phase 003 worktree. Each launch read
back `eula=true`, bound only `127.0.0.1:25589`, retained online authentication, and disabled RCON
and query. The before, after and production PIDs 1052577, 1058244 and 1063132 exited with code 0.
Cleanup verified their absence, no remaining process working inside the runtime, and the free port.
It removed 1,127 new build paths and 13 new local Gradle paths, restoring the exact 836 and 26 path
baselines. The runtime's symlink to preexisting `run-248/libraries` was unlinked without following
it. The candidate, source, shared caches, preexisting runtime and unrelated worktrees were preserved.
The eight private scratch files were removed after this sanitized record was saved.

This closes the reproduced collaborator session defect. The remaining source and dependency
security review, real provider behavior, browser and client workflows, broader lifecycle gates
and final checked integration remain open. The immutable goal and phase cursor were unchanged.

## Selling Bin component and bypass client followup

On September 13, 2026, the same packaged `8cdb8e08ef3b16cbe5cb09712a29a69e47f06ba2`
candidate was exercised through the authenticated node-1 dedicated server and matching silent
laptop client on NeoForge 21.1.248. The [component and bypass record](selling-bin-interaction-repair.md#component-offhand-and-bypass-followup)
adds actual main hand component preservation, sneaking denial, creative bypass enablement and
disablement, component preserving offhand GUI insertion, revoke with an open menu, previously
accepted deposit sales, selective carrot sales and resource reload evidence. Exact state,
capture records, artifact identities, owned input and images are in
[the bounded observations](selling-bin-client/components-20260913.json).

The checks passed without a source change. They extend BIN-AC-002D, BIN-AC-002E and
BIN-AC-004E; they do not complete all hand combinations, multiple real players, current provider
retention, the packaged Brave authoring journey or final integration. The existing dependency
warning and broader security gates remain open. Both hosts completed cleanup and restored the
reused client instance. The saved goal and phase cursor were unchanged.

## Editor package import followup

On September 13, 2026, the server import boundary failed two regressions on the previous
`dfd3ca14663008b60482864e8f5be7c6e3478a21` source plus the new fixture classes. A package containing
`stage.toml` followed by `script.js` returned an error after changing the draft revision. Exporting
and reimporting a package containing nested and helper TOML files also failed. These were actual
GameTest assertion failures at 09:13:55 and 09:14:02 America/Chicago. Earlier launcher attempts
failed because the development classpath was incomplete or the template namespace was disabled;
those setup attempts did not execute tests.

Source `e9cd17b3385d0ffea47f61b6fc2cbcb269e22ea1` addresses the import regressions within
BIN-AC-015B and BIN-AC-015C. It validates all package paths before mutation, rejects unsafe paths and normalized duplicates, and imports
nested and helper TOML files unchanged. The draft monitor covers the mutation sequence. Existing
per file undo and redo semantics remain. [The import test guide](../test/editor-import.md) describes
the exact fixtures and their cleanup.

`./gradlew test build prepareServerRun` passed with Java 21 and NeoForge 21.1.248. All 415 Java tests
passed, with zero failures, errors or skips. The inspected task graph launched no client. On the
fixed dedicated development server, the following actual tests passed. Each pass was verified by
the lime stained glass result block and matching structure metadata after the test ran.

| Test | Structure position | Result position |
| --- | --- | --- |
| `packageimportrejectsinvalidfilesatomically` | `0 180 3` | `-1 179 2` |
| `packageexportreimportsnestedtoml` | `20 -60 3` | `19 -61 2` |
| `removedcollaboratorsloseeverysessionaction` | `40 -60 3` | `39 -61 2` |
| `sessionapplyrejectsunreviewedchanges` | `60 -60 3` | `59 -61 2` |

The import tests cover script and traversal rejection, absolute and Windows drive paths, normalized
path aliases, exact export and reimport contents, compilation of the included bread rule, and undo
and redo. The existing authorization and apply regressions also passed. Their cleanup restored
50 live showcase definitions. The fixture player objects establish server service behavior;
no browser or authenticated client workflow was exercised in this suite.

After the signed source commit, `./gradlew build` passed again. The candidate records that commit
with `Build-Dirty: false`. All 798 project class files matched the tested compiled bytes.
The resulting `progressivestages-3.0.5.jar` has these hashes:

```text
SHA-256 a74fee8c16b9d0bd239807a47861bb699a0e454eca0a77c279cf36dd7df49afc
SHA-512 4540dd1ba094be7e4eab2a8058904f328cd741b25a6cbd89a6fb9e4dfa7172fa1b334c015a62415af9e36c060570b77e252a36bd12b5d8c19d9c5c41aa301bc3
```

The packaged production server reached readiness at 09:18:18 with Minecraft 1.21.1, NeoForge
21.1.248 and 50 loaded definitions, then stopped normally. Development and production launches
used node-1, Debian OpenJDK 21.0.11+10 and the owned `build/editor-import-runtime` inside the
phase 003 worktree. They bound only `127.0.0.1:25589`, retained online authentication, disabled
RCON and query, and read back `eula=true` before launch. The actual development baseline, fixed
and production PIDs were 1146248, 1149733 and 1154752. All exited with code 0. The two earlier
setup PIDs 1136878 and 1137987 also exited before their runtime was reused.

Cleanup verified all five PIDs absent, no process working in the runtime and the port free. It
removed 1139 new build paths and 23 new local Gradle paths,
restoring the exact 836 and 26 path baselines. The runtime library symlink was unlinked without
following it. The candidate, source, preexisting `run-248` libraries, shared caches and unrelated
worktrees were preserved. Private scratch files were removed after retaining this sanitized record.

This closes the reproduced import defect, not the entire builder or security gate. The current
GitHub dependency summary remains 33 open alerts, including one critical and fifteen high alerts.
No alert was dismissed and no dependency was upgraded. Actual Brave package journeys, provider
behavior, remaining lifecycle and security checks, and final checked integration remain open.
DEC-006 already required 21.1.248 throughout the plan. Maintenance validation passed without
changing any plan file, handoff, immutable goal or active phase cursor.

## External configuration conflict followup

On September 13, 2026, the actual editor session apply path reproduced an external file overwrite
on the `aa24b211f631100cada7bd16f5e9d765f5542a32` baseline plus the new regression fixture. The fixture
loaded a stage, opened and reviewed an edited draft, then changed the live file without a reload.
At 09:26:08 America/Chicago, `sessionapplyrejectsexternalchangesbeforereload` failed because the
apply did not return `configuration_conflict`. The old condition compared live files only after a
compiled configuration revision change.

Signed source `0bfc7750c5e34f65da32f0a30eeeab0e477204e9` now compares live files with the draft base on every apply. The
[external edit regression](../test/editor-external-changes.md) confirms that rejection preserves
the external file, draft source, draft revision and last valid compiled revision. The existing direct
apply fixture now supplies the actual initial file snapshot, matching the real session precondition.
The change addresses BIN-AC-014B and BIN-AC-015B without adding a setting or changing a version pin.

All 415 Java tests passed with zero failures, errors or skips. `./gradlew test build prepareServerRun`
and the later clean source `./gradlew build` passed. Five relevant GameTests passed on the exact
NeoForge 21.1.248 development server. Structure metadata and lime stained glass established each result:

| Test | Structure position | Result position |
| --- | --- | --- |
| `sessionapplyrejectsexternalchangesbeforereload` | `0 -60 3` | `-1 -61 2` |
| `sessionapplyrejectsunreviewedchanges` | `20 -60 3` | `19 -61 2` |
| `applywritesreloadsandrestorescanonicalrules` | `40 -60 3` | `39 -61 2` |
| `invalidrecipealiasdoesnotmutateliveconfiguration` | `60 -60 3` | `59 -61 2` |
| `contextapplypreservessourceandrejectsoverflow` | `0 180 3` | `-1 179 2` |

Earlier context attempts used a distant location whose result could not be verified. They are not
accepted evidence. The final context run used a prepared platform near spawn and produced
`context_apply_loaded_pass` with matching metadata at 09:35:14. The command selects terrain height,
so a supplied Y coordinate alone did not establish the earlier result position.

The final candidate records the source commit above and `Build-Dirty: false`. All 799 project
class files match the tested compiled bytes. Its hashes are:

```text
SHA-256 980da9cc3621032a3b4b3d5d3631aca3d758376d7efbd4ae1d62dd8bf3e88bed
SHA-512 dd239c09b8641bad46fb17ed37a15c3abd9f89d1178903b2b7053e725691d31efc43c80a935ea1ae78203d71354742410f18caf275e15d8fc37b88b10bbd919e
```

The packaged production server reached readiness at 09:31:26 with 50 loaded stage definitions and
stopped normally. A preceding setup attempt omitted the candidate because the existing mods
folder interrupted installation. That attempt was stopped and excluded; the candidate was then
installed and hash verified before the accepted launch. All launches were headless on node-1,
with Minecraft 1.21.1, NeoForge 21.1.248, Debian OpenJDK 21.0.11+10, online authentication, RCON and
query disabled, and the private listener `127.0.0.1:25589`. Every launch read back `eula=true` in
the owned `build/editor-external-runtime` under the phase 003 worktree. No laptop runtime was used.

[Quality run 34762824035](https://github.com/MCEnvision/ProgressiveStages/actions/runs/34762824035)
completed successfully on the exact source commit. Gradle, frontend, secret scan, dependency
submission and both Java and JavaScript CodeQL jobs passed. Dependency review and documentation
jobs were skipped by the existing workflow and are not counted as executed checks. The targeted
local documentation link check and `git diff --check` passed.

The successful CodeQL run is not a clean finding list. The branch query returned 133 open findings:
63 command permission patterns, 39 packet handler patterns, 16 network string patterns, nine path
resolution patterns, four sensitive logging findings and two draft synchronization findings.
Alerts 146 and 147 refer to `EditorApplyService`; its normal session caller holds the draft monitor
through apply, which must be included in their call path review. These findings and the 33 dependency
alerts remain open pending scoped disposition. No findings were dismissed to obtain a passing run.

Cleanup verified all six owned server PIDs absent, no process working in the runtime and the private
port free. PIDs 1166405, 1169519, 1173516, 1175212, 1176204 and 1180463 exited normally. Cleanup removed
1081 new build paths and 18 new local Gradle paths, restoring
exactly 836 and 26 baseline entries. It unlinked the runtime's library symlink without following it.
Source, the candidate, preexisting `run-248` libraries, shared caches and unrelated worktrees remain.
Private scratch files were removed after this sanitized record was retained. The remote jobs are
complete; their required CI evidence remains on GitHub. Browser presentation, provider acceptance,
remaining security and lifecycle gates, and the final merge remain open. The goal and cursor are unchanged.
