# Phase 000 Execution Plan

> **Plan ID:** PLAN-PHASE-000
> **Phase ID:** CORE-PHASE-000
> **Owner:** RepositoryAudit
> **Classification:** MANDATORY
> **Master plan:** [plan.md](../plan.md)
> **Phase sequence:** 000 of 006

## Purpose and Ownership

This phase freezes the defect, inventory-interaction seam, and artifact baseline for the 3.0.4 polish release. It owns only CORE-REQ-001: evidence capture, reproduction or artifact verification of issues `#8`, `#10`, `#11`, `#16`, `#24`, and `#25`, evidence-based classification of any stale report, reconciliation of advertised 3.0.3 behavior with the shipped artifact, and traceable acceptance mapping for later phases. Separately, it freezes SRC-009 and the present implementation seams that CORE-REQ-012 may extend; this owner-promoted feature is mandatory scope but is not a report and must never be represented, reproduced, classified, closed, or counted as a seventh baseline issue. The issue `#25` audit distinguishes the canonical recipe-output field `[recipes].locked_items`, the separate exact-recipe field `[recipes].locked_ids`, and the forbidden generic field `[recipes].locked`, but the corrected serializer and denied-versus-eligible runtime proof belong first to CORE-PHASE-003. The master plan owns product scope, phase order, owner decisions, external prerequisites, and the final release endpoint. This blueprint owns the detailed audit procedure and evidence packet needed to route downstream implementation without requiring a future corrected candidate to exist during baseline capture.

Phase state is explicit. The phase is `incomplete` while a required identity, six-row classification, claim result, seam record, mapping, or consistency check is absent or unsupported. It is `blocked` only when a required Phase 000 evidence source cannot be accessed or attributed and no bounded artifact inspection can classify the row. It may complete with an issue row classified `blocked_by_missing_evidence` when the missing high-fidelity proof is named, assigned to its downstream owner, and is not required to establish the current baseline. Corrected candidate behavior never substitutes for or gates the historical baseline audit.

## Evidence-Based Entry State

| Evidence class | Area | Finding | Source or command | Freshness condition |
|---|---|---|---|---|
| VERIFIED | Repository baseline | The authoritative audit baseline is the saved-goal checkout `origin/envy/polish-3.0.4-plan` at `5b3077764907249b3711886cca538794f6139acf` | Master plan §§1 and 4 and saved-goal creation provenance | Invalid only if repository, remote, or commit provenance disproves that identity; later phase-branch commits do not redefine the frozen audit baseline |
| VERIFIED | Issue baseline | The mandatory report set is exactly issues `#8`, `#10`, `#11`, `#16`, `#24`, and `#25` | Master plan §4, SRC-004, SRC-008, and DEC-006 | Invalid if the owner formally revises the locked six-issue baseline |
| OBSERVED | Entity-presence performance | Issue `#24` reports condition-context construction in the entity tracking decision path as the server-thread hotspot | SRC-006 and master plan §4 | Invalid if the supplied profile is shown to identify another build, configuration, or code revision |
| OBSERVED | Curios integration | Issue `#8` reports that Curios 9.5.1 slot gating does not resolve the supported API surface | SRC-004 and master plan §4 | Invalid if the report used a different Curios, Minecraft, NeoForge, or ProgressiveStages artifact |
| VERIFIED | Recipe-viewer configuration | Existing evidence does not prove independent JEI and EMI controls or their combined installation behavior | SRC-003 and master plan §4 | Invalid when a shipped-artifact matrix proves all DEC-003 combinations |
| UNKNOWN | Easy Builder enchantments | Source presence does not yet prove that the production editor bundle and shipped JAR expose the advertised controls | Issue `#11`, SRC-003, and master plan §4 | Resolved only by production-bundle and shipped-artifact workflow evidence |
| OBSERVED | Easy Builder recipe locks | Issue `#25` reports that a browser-editor recipe-output rule is ineffective or appears to disappear. Source inspection identifies `[recipes].locked_items` and `[recipes].locked_ids` as distinct recognized fields. The exact production-bundle serialization remains unresolved until this phase records it. | Issue `#25`, SRC-008, `RulesPanel.tsx`, `EditorSchemaRegistryTest`, `StageFileParser`, and master plan §4 | Resolved only by a pinned production-bundle round trip through visual form, TOML, persistence, reload, compiler activation, and runtime enforcement |
| VERIFIED | Inventory insertion source role | SRC-009 is an owner request that promotes player-initiated destination-aware inventory insertion gating to CORE-REQ-012; it is outside the fixed six-issue report matrix | SRC-009, DEC-007, FUT-003, CORE-REQ-012, and master plan §§2, 6, 9, and 12 | Invalid only if the owner revises the product contract; implementation evidence may refine the seam map but cannot convert this feature into a baseline issue |
| OBSERVED | Inventory insertion seams | Existing `[[interactions]]` support `item_on_block`, `block_right_click`, and `item_on_entity` with one held-item selector and one world target. `StageFileParser.parseInteractions` and `LockDefinition.InteractionLock` represent that legacy surface; `Schema4StageCompiler.addGenericRules` compiles the current single-category selector shape; `AbstractContainerMenuMixin` gates existing locked-item movement and hotbar placement without resolving a destination inventory owner or preserving a paired source-and-destination decision; `BuiltinEditorSchemas` and `RulesPanel.tsx` expose and serialize one category selector per rule; existing schema and regression tests do not establish the CORE-REQ-012 two-selector, resolver, click-path, conservation, automation, multiplayer, reload, or packaged-editor contract | `StageFileParser.parseInteractions`, `LockDefinition.InteractionLock`, `Schema4StageCompiler.addGenericRules`, `AbstractContainerMenuMixin`, `BuiltinEditorSchemas`, `RulesPanel.tsx`, `EditorSchemaRegistryTest`, SRC-003, SRC-009, and master plan §§4, 11, and 12 | Invalid when a different pinned baseline changes one of these seams; superseding evidence must identify the changed source or artifact identity without rewriting the original observation |
| UNKNOWN | Category overlay | Source ordering changes do not yet prove correct depth and input behavior at supported GUI scales | Issue `#16`, SRC-003, and master plan §4 | Resolved only by shipped-artifact client runtime evidence |
| UNKNOWN | Advertised 3.0.3 parity | Public 3.0.3 claims have not been completely reconciled with the shipped JAR | SRC-001, SRC-002, SRC-003, and CORE-REQ-007 | Invalid when a claim-by-claim artifact matrix is complete for the pinned release |

## Scope Boundaries

### Included Scope

- CORE-REQ-001 — Freeze the pinned repository revision, installed artifact identity, six issue reports, relevant configurations, expected behavior, observed behavior, and evidence classification.
- CORE-REQ-001 — Reproduce each report when its historical environment is available. Otherwise artifact-verify it, classify it stale only with explicit contradictory artifact evidence, or use `blocked_by_missing_evidence` with the missing proof and downstream owner recorded.
- CORE-REQ-001 — Inspect the shipped 3.0.3 artifact and public 3.0.3 documentation claim by claim, then record only proven missing advertised capabilities for CORE-REQ-007.
- CORE-REQ-001 — Map every confirmed or artifact-verified outcome to exactly one owning corrective requirement from CORE-REQ-002 through CORE-REQ-007 or CORE-REQ-011 and its measurable acceptance evidence.
- CORE-REQ-001 — For issue `#25`, freeze the reporter workflow and artifact evidence needed by CORE-PHASE-003 to repair recipe-output serialization through `[recipes].locked_items`, retain `[recipes].locked_ids` for exact recipe identifiers, forbid successful persistence of `[recipes].locked`, and prove failed validation or migration preserves the last valid rule.
- CORE-REQ-001 — Freeze SRC-009 as the source role for the separately owner-promoted CORE-REQ-012 feature and audit the current legacy-interaction, selector, compiled-rule, menu-transaction, destination-identity, target-catalog, editor-serializer, and regression-test seams that CORE-PHASE-003 may change.
- CORE-REQ-001 — Produce a Phase 003 handoff that identifies present behavior, absent capabilities, preservation constraints, authoritative source locations, test boundaries, and required CORE-REQ-012 evidence without selecting or implementing the repair.
- CORE-REQ-001 — Preserve the evidence contracts for EXT-001, EXT-002, and EXT-003 without claiming those later prerequisites are complete.

### Explicit Exclusions

- CORE-REQ-002 through CORE-REQ-012 are downstream implementation, compatibility, release-validation, publication, and issue-closure work. This phase may assign evidence or a seam handoff to them but may not execute them.
- FUT-001, FUT-002, FUT-003, and FUT-004 remain excluded and cannot be promoted through the artifact audit.
- NG-001 excludes a progression redesign, broad schema overhaul, or any lock category beyond the bounded inventory-interaction extension in CORE-REQ-012.
- NG-002 excludes converting Curios, JEI, EMI, or another integration into a hard dependency.
- NG-003 excludes adding runtime scans or performance instrumentation to production hot paths during the audit.
- NG-004 prohibits treating source presence, release notes, or a lower-fidelity check as sufficient issue-closure evidence.
- Corrective source, resource, configuration, documentation, workflow, release, or GitHub issue changes are outside this phase.

## Phase Contract

### CORE-PHASE-000 — Freeze the defect, inventory-interaction seam, and artifact baseline

**Objective:** Produce a reproducible, revision-pinned audit matrix that classifies all six baseline reports, reconciles every advertised 3.0.3 capability with the shipped artifact, separately freezes the SRC-009 inventory-insertion implementation seams, and assigns every proven gap or feature seam to a downstream requirement with measurable acceptance evidence
**Owner:** RepositoryAudit
**Dependencies:** none
**Canonical requirements:** CORE-REQ-001
**Documentation and release impact:** Produce the versioned audit matrix, artifact inventory, claim comparison, reproduction records, sanitized evidence index, and downstream acceptance mapping; do not update release-facing documentation or publish artifacts in this phase
**Next transition:** CORE-PHASE-001

**Entry criteria**

- The authoritative baseline remote, branch, and commit match the identities in the master plan.
- The six report records and their supplied attachments, configurations, or screenshots are available in sanitized form, or their absence is explicitly recorded.
- The shipped 3.0.3 JAR and release identity are uniquely identified, its source revision is recorded only when release provenance proves it, and the public documentation and release claims used for comparison are uniquely identified.
- Test identities record ProgressiveStages, Minecraft 1.21.1, NeoForge 21.1.219, and each optional integration version when applicable.
- DEC-002 remains the scope boundary: a proven missing advertised or documented 3.0.3 capability is mandatory, while an unpromised feature remains excluded.
- SRC-009 and DEC-007 are pinned as the sole owner-promotion basis for CORE-REQ-012, and the evidence model keeps that feature seam separate from the six report rows.

**Implementation scope**

- CORE-REQ-001 only: freeze evidence, execute reproductions and artifact inspections, classify reports, record advertised-capability parity, and trace outcomes to downstream acceptance gates.
- CORE-REQ-001 preserves raw evidence separately from conclusions so another maintainer can repeat each classification from the same artifact identities and test inputs.
- CORE-REQ-001 records unknowns and unavailable evidence as blockers or downstream prerequisites; it does not convert uncertainty into a confirmed defect, stale report, or completed prerequisite.
- CORE-REQ-001 records the present interaction parser, legacy model, selector matcher and compiler, menu enforcement, stable-target catalog, editor serializer, and test boundaries as observational evidence. It does not design, code, or validate the future CORE-REQ-012 implementation.

**Execution order**

1. `P000-TASK-001` executes CORE-REQ-001 baseline identity and evidence-source freezing.
2. `P000-TASK-002` executes CORE-REQ-001 issue-by-issue reproduction planning and normalized observation capture.
3. `P000-TASK-003` executes CORE-REQ-001 shipped 3.0.3 JAR, public-claim inventory, and current CORE-REQ-012 implementation-seam inventory.
4. `P000-TASK-004` executes CORE-REQ-001 evidence-based classification and stale-report review.
5. `P000-TASK-005` executes CORE-REQ-001 advertised-capability gap review under DEC-002.
6. `P000-TASK-006` executes CORE-REQ-001 downstream requirement and acceptance-evidence mapping, including the distinct SRC-009 to CORE-REQ-012 to CORE-PHASE-003 handoff.
7. `P000-TASK-007` executes CORE-REQ-001 audit consistency review and completion-packet freeze.

**Required evidence**

- A versioned audit matrix with one row per baseline report and one row per advertised 3.0.3 capability claim examined.
- A final shipped-JAR inventory tied to artifact hash, release identity, and source revision when provenance evidence establishes that relationship.
- Reproduction records that identify environment, configuration, fixture, steps, expected behavior, observed behavior, logs or screenshots, rerun count, and result.
- A classification rationale for every report using only `reproduced`, `artifact_verified`, `stale_with_evidence`, or `blocked_by_missing_evidence`.
- A dedicated issue `#25` baseline record showing the reporter intent, the exact visual selector path available in the shipped editor, generated payload or TOML, validation response, persistence and reopen observations, and parser or compiler interpretation obtainable from the pinned artifact. It must distinguish `[recipes].locked_items`, `[recipes].locked_ids`, forbidden `[recipes].locked`, and any generic `[[rules]]` representation actually emitted. Missing corrected-field, reload, or denied-versus-eligible runtime proof is recorded as CORE-PHASE-003 work rather than invented or required for Phase 000 closure.
- An issue traceability section containing exactly six report rows and mapping each report outcome to CORE-REQ-002 through CORE-REQ-006 or CORE-REQ-011, the responsible later phase, acceptance criteria, and required proof. Proven advertised-capability gaps use separate claim rows mapped to CORE-REQ-007 and do not change the issue count.
- A separate SRC-009 feature-seam record mapping CORE-REQ-012 to CORE-PHASE-003 without adding a report row. It must name `StageFileParser.parseInteractions`, `LockDefinition.InteractionLock`, `Schema4StageCompiler.addGenericRules`, `AbstractContainerMenuMixin`, `BuiltinEditorSchemas`, `RulesPanel.tsx`, `EditorSchemaRegistryTest`, and any additional pinned regression fixture found during the audit, and state each seam's present role, preserved legacy behavior, absent CORE-REQ-012 capability, evidence surface, and downstream proof obligation.
- A current-state inventory insertion seam map covering legacy `[[interactions]]` types and fields; the existing `all`, `id`, `mod`, `tag`, `name`, and `#` selector grammar and safe priority policy; source-selector compilation; the absence of a paired destination selector and stable block, menu, or inventory-owner resolver; standard click, quick move, quick craft, hotbar swap, and pickup-all transaction entry points; player-origin and playerless automation boundaries; editor target catalogs and serializer shapes; reload and last-valid-snapshot behavior; and existing parser, compiler, editor, menu, conservation, automation, multiplayer, and custom-menu test coverage or gaps.
- An external-boundary register that leaves EXT-002 unavailable and EXT-003 unauthorized until their exact master-plan evidence contracts are satisfied, and that records EXT-001 artifact evidence without using it as proof of runtime success.

**Exit criteria**

- All six baseline issues have an explicit, supported classification and no report is marked stale solely because source appears to contain a possible fix.
- Every audited public 3.0.3 claim has an artifact-backed result, or an explicit missing-evidence blocker with an owner and downstream resolution gate.
- Every confirmed defect or advertised-capability gap maps to one downstream canonical requirement and measurable verification evidence.
- The separate CORE-REQ-012 seam map is complete enough for CORE-PHASE-003 to implement the two-selector schema, authoritative destination resolver, complete player menu transaction gate, playerless automation boundary, editor target catalogs and serialization, reload behavior, and required tests without rediscovering present ownership or mistaking the feature for a seventh issue.
- The audit contains no novel feature request beyond the explicitly owner-promoted CORE-REQ-012 seam, platform upgrade, speculative defect, credential, private raw log, or unsupported completion claim.
- An independent consistency review can reproduce the matrix classifications from the pinned artifacts and evidence index.
- The phase completion packet is immutable by identity: later discoveries append superseding evidence instead of silently rewriting the frozen baseline.
- Any issue row left `blocked_by_missing_evidence` names the unavailable proof, explains why the current classification remains honest, and assigns the exact later evidence gate. A row-level missing proof does not become a false phase-completion claim.
- No known mandatory phase-owned defect remains.

## Inputs and Upstream Contracts

| Input or contract | Provider | Required state | Validation | Failure behavior |
|---|---|---|---|---|
| Locked owner request and decisions | EnVy | DEC-001 through DEC-007 retain the selected choices in the master plan | Compare planning subject, endpoint, scope rule, recipe-viewer decision, performance gate, release gate, six-issue baseline, and distinct CORE-REQ-012 promotion with the master plan | Stop classification that would expand scope and route any contract conflict to the owner |
| Repository identity | origin/envy/polish-3.0.4-plan | Remote, ref, and commit equal the master-plan baseline | Record remote URL, ref, commit, clean inspection state, and collection time | Stop the audit if identity cannot be proven; do not substitute another branch or working tree |
| Six issue reports | SRC-004 and SRC-008 | Exactly `#8`, `#10`, `#11`, `#16`, `#24`, and `#25`, including available attachments and reporter configurations | Record issue identity, update timestamp, affected version claim, and sanitized evidence inventory | Mark only the unavailable evidence field blocked; never infer report details |
| Inventory insertion owner request | SRC-009 | The SuperDevyn profession and selling-bin use case promotes CORE-REQ-012 separately from the fixed six-issue baseline | Record source-role identity, DEC-007 relationship, canonical requirement, non-issue status, and master-plan acceptance boundary | Stop if the request cannot be separated from the issue matrix or if evidence would broaden CORE-REQ-012 beyond the master contract |
| Recipe-lock field contract | SRC-008 and master plan CORE-REQ-011 | Recipe-output selection targets `[recipes].locked_items`; exact recipe identifiers target `[recipes].locked_ids`; generic `[recipes].locked` is never accepted as a successful canonical recipe lock | Inspect the shipped bundle and artifact for the reporter path, record exactly what it emits and what the current parser or compiler recognizes, and label unavailable runtime observations as downstream proof | If intent cannot be inferred without ambiguity, record the required atomic rejection and preservation behavior for CORE-PHASE-003; never claim the current artifact performed behavior that was not observed |
| Performance report | SRC-006 | Profile identity and configuration are attributable to issue `#24` | Record artifact version, fixture, configuration, profile identity, and relevant call-path observation | Classify the performance report as blocked if artifact or fixture identity is insufficient |
| Shipped 3.0.3 artifact | Release provenance | Artifact is uniquely identified by version, platform, source revision when provable, SHA-256, and SHA-512 | Recalculate hashes, inspect JAR inventory, and compare embedded metadata | Reject an ambiguous, rebuilt, or hash-mismatched artifact from parity conclusions |
| Public 3.0.3 claims | SRC-002 and SRC-003 | Claims are bounded to published documentation and release material attributable to 3.0.3 | Capture claim text location, publication identity, and advertised user-visible outcome | Exclude ambiguous roadmap language from mandatory parity scope |
| Optional integration artifacts | EXT-001 | Evidence records authoritative source, compatibility, exact version, license provenance, security review, SHA-256, and SHA-512 | Verify the seven typed artifact-evidence fields before using an artifact in a reproduction | Record the missing field and defer affected runtime proof; do not relax the evidence contract |
| Release workflow blocker | EXT-002 | Availability remains `unavailable` until the corrected shared workflow revision and disposable-candidate proof exist | Preserve the exact prerequisite identity and required evidence from the master plan | Keep CORE-REQ-009 and CORE-REQ-010 externally blocked; do not bypass or relabel the prerequisite |
| Publication authority | EXT-003 | Authorization remains `not_authorized` until bound to artifact identities, operations, operators, rollback, runbook digest, systems, and time window | Preserve all seven authorization scope-binding fields | Keep publication prohibited; never treat earlier codes or unscoped approval as valid |

## Outputs and Downstream Contracts

| Output or contract | Consumer | Guaranteed state | Compatibility or versioning | Evidence |
|---|---|---|---|---|
| Frozen issue matrix | CORE-PHASE-001 through CORE-PHASE-003 | Every baseline report has a pinned identity, observation record, classification, and owning requirement | Append-only supersession preserves the original baseline and explains any later evidence change | Matrix version, evidence index, reviewer record, and artifact hashes |
| Entity-presence fixture contract | CORE-PHASE-001 | Issue `#24` expected behavior, active rule configuration, multiplayer roles, baseline metric, and reported hotspot are reproducible without prescribing the repair | Fixture remains tied to the audited artifact and DEC-004 thresholds | Reproduction record and profile provenance |
| Optional-integration matrix seed | CORE-PHASE-002 | Issues `#8` and `#10` identify exact integration artifacts, configuration states, expected behavior, and observed behavior | EXT-001 evidence fields remain mandatory and integrations remain optional | Artifact evidence register and reproduction records |
| Client and editor fixture contract | CORE-PHASE-003 | Issues `#11`, `#16`, and `#25` identify artifact, UI state, test content, GUI scale or editor workflow, expected behavior, and observation | Evidence distinguishes source, development bundle, shipped bundle, and installed JAR | Screenshots or recordings, browser or client logs, generated TOML, persisted-file digests, and artifact inventory |
| Recipe-lock repair handoff | CORE-PHASE-003 | Issue `#25` is assigned to CORE-REQ-011 with a pinned failing or missing-capability workflow and distinct expected behavior for `[recipes].locked_items`, `[recipes].locked_ids`, `[recipes].locked`, and any observed generic representation | Phase 000 preserves historical evidence. CORE-PHASE-003 alone owns corrected serialization, migration or rejection, reload, runtime crafting, and rollback proof against the 3.0.4 candidate | Baseline bundle identity and emitted data, plus an explicit Phase 003 checklist for file digests, validation, reopen, compiler activation, reload, denied and eligible crafting, and preservation |
| Inventory insertion seam map | CORE-PHASE-003 | SRC-009 remains a non-issue owner request mapped to CORE-REQ-012, while every current parser, model, compiler, selector, menu transaction, target catalog, editor serializer, and test seam has a pinned present-state role and preservation constraint | The map is observational and append-only. It preserves legacy `[[interactions]]` behavior and cannot prescribe a competing schema or count CORE-REQ-012 as a report | Source locations, current call or data-flow notes, field shapes, catalog identities, test inventory, gap classification, baseline commit, and reviewer record |
| CORE-REQ-012 Phase 003 handoff | CORE-PHASE-003 | The downstream owner receives the current single-selector interaction path, missing paired-destination and resolver boundaries, all menu transaction entry points, authenticated-player and playerless-automation boundaries, editor catalog and serializer gaps, legacy regression obligations, and every master-plan acceptance and evidence gate | CORE-PHASE-003 must preserve current `block_right_click`, `item_on_block`, and `item_on_entity` forms and implement only the additive canonical `item_into_inventory` contract from the master plan | Seam-to-task trace showing one Phase 003 owner for each parser, compiler, resolver, menu, editor, catalog, reload, diagnostics, compatibility, and test obligation |
| Advertised-capability parity matrix | CORE-PHASE-003 | Every proven missing 3.0.3 claim is bounded to a public source and assigned to CORE-REQ-007 | Novel or roadmap-only features remain excluded under DEC-002 | Claim source, artifact observation, classification, and traceability row |
| Acceptance traceability matrix | CORE-PHASE-001 through CORE-PHASE-006 | Each proven gap names exactly one owning requirement, later phase, acceptance condition, and evidence type; SRC-009 maps separately to CORE-REQ-012 and CORE-PHASE-003 | Changes require explicit superseding evidence, never silent reassignment or issue-count inflation | Cross-check report showing no orphan or duplicate ownership and exactly six baseline issue rows |

## Work Packages

| Task ID | Requirement IDs | Work | Inputs and dependencies | Outputs | Affected components or interfaces | Verification |
|---|---|---|---|---|---|---|
| P000-TASK-001 | CORE-REQ-001 | Freeze repository, release artifact, documentation, issue, optional dependency, SRC-009 feature-request, and evidence identities before interpretation | SRC-001 through SRC-009, DEC-001 through DEC-007 | Baseline manifest and sanitized evidence index with separate issue and feature-request sections | Repository baseline, release provenance, issue records, owner-request record, evidence store | Identity cross-check proves commit, artifact hashes, exactly six issues, separate SRC-009 role, and all source roles match the master plan |
| P000-TASK-002 | CORE-REQ-001 | Define and execute bounded historical reproductions for each issue using the reporter configuration when available, including the issue `#25` recipe-output intent and current rule-preservation boundary | P000-TASK-001, issues `#8`, `#10`, `#11`, `#16`, `#24`, `#25` | Exactly six issue records with expected behavior, observed behavior, evidence surface, rerun count, classification candidate, and missing proof | Entity-presence fixture, Curios fixture, recipe-viewer fixture, enchantment Easy Builder workflow, category-overlay workflow, recipe-lock Easy Builder workflow | Repeated runs are used when the environment exists; otherwise a pinned artifact inspection or `blocked_by_missing_evidence` record preserves the gap. Issue `#25` evidence distinguishes output-item intent, exact recipe intent, forbidden generic fields, and the actual emitted representation without requiring the future repair |
| P000-TASK-003 | CORE-REQ-001 | Inventory the shipped 3.0.3 JAR, enumerate attributable public user-visible claims, and freeze the current CORE-REQ-012 implementation seams without implementing the feature | P000-TASK-001, shipped artifact, SRC-002, SRC-003, SRC-009, master plan §§4, 11, and 12 | JAR inventory, public-claim register, inventory insertion seam map, and existing-test inventory | `StageFileParser.parseInteractions`, `LockDefinition.InteractionLock`, `Schema4StageCompiler.addGenericRules`, selector matcher registry, `AbstractContainerMenuMixin`, target and editor catalogs, `BuiltinEditorSchemas`, `RulesPanel.tsx`, schema tests, menu tests, and packaged editor surfaces | Hash recheck and claim-source review prove artifact identities; seam review records present role, supported legacy behavior, missing paired-selector or destination behavior, and existing or absent regression proof for every boundary |
| P000-TASK-004 | CORE-REQ-001 | Classify each issue from reproduction and artifact evidence, including a strict stale-report test | P000-TASK-002, P000-TASK-003, NG-004 | Classification table with rationale and missing-evidence fields | Issue audit matrix | Reviewer can derive each classification without source-presence assumptions |
| P000-TASK-005 | CORE-REQ-001 | Compare every bounded 3.0.3 claim with the shipped artifact and separate proven omissions from novel or roadmap-only ideas | P000-TASK-003, P000-TASK-004, DEC-002, FUT-001 | Advertised-capability parity matrix | Public contract, shipped JAR, production editor bundle | Each mandatory row has a public claim and artifact observation; excluded rows state why they are not promised 3.0.3 behavior |
| P000-TASK-006 | CORE-REQ-001 | Map confirmed outcomes and the separately promoted inventory-insertion seam to downstream requirements and measurable acceptance proof | P000-TASK-003, P000-TASK-004, P000-TASK-005, CORE-REQ-002 through CORE-REQ-007, CORE-REQ-011, CORE-REQ-012 | Acceptance traceability matrix, phase fixture handoffs, and CORE-PHASE-003 inventory-insertion handoff | Requirements and phase boundaries | Completeness check finds no confirmed gap without one owner and no downstream requirement without relevant baseline input; issue `#25` maps to CORE-REQ-011 and CORE-PHASE-003, while SRC-009 maps separately to CORE-REQ-012 and CORE-PHASE-003 without adding an issue row |
| P000-TASK-007 | CORE-REQ-001 | Audit matrix consistency, issue-count integrity, seam-map completeness, sensitive-data sanitation, external-boundary preservation, and completion-packet integrity | P000-TASK-001 through P000-TASK-006, EXT-001, EXT-002, EXT-003 | Frozen Phase 000 completion packet | Evidence store and downstream handoff | Independent review confirms identities, hashes, exactly six issue classifications, separate SRC-009 handling, present seam roles, exclusions, traceability, and external prerequisite states |

Tasks execute in numeric order because interpretation depends on frozen identities, classification depends on reproduction and artifact inspection, and downstream mapping depends on classification and the frozen seam map. Within P000-TASK-002, independent issue reproductions may run in parallel after P000-TASK-001. Within P000-TASK-003, JAR inventory, public-claim collection, source seam inspection, packaged editor inspection, and existing-test inventory may run in parallel after identities are fixed. A failed or unavailable historical reproduction does not roll back the baseline manifest; it becomes `blocked_by_missing_evidence` with the exact missing input and downstream proof recorded. Phase 000 remains incomplete only if that classification itself is unsupported or unmapped. An unavailable seam is recorded as an audit gap for Phase 003, never as a seventh issue. Any exposed sensitive data is removed from the evidence packet and the affected capture is repeated from a sanitized source.

## Architecture and Implementation Boundaries

Phase 000 is observational. It does not change runtime architecture, schemas, persistence, networking, configuration defaults, permissions, client rendering, optional adapters, editor code, or release workflows. RepositoryAudit owns evidence normalization and traceability but does not own a runtime repair.

All observations distinguish the logical server, client presentation, optional adapter, editor bundle, and release tooling boundaries defined in the master plan. Evidence must identify whether it came from source inspection, a development runtime, a production editor bundle, the shipped JAR, an integrated server, or a dedicated server. Source inspection may identify a hypothesis or candidate implementation, but only the required runtime or artifact workflow may prove user-visible behavior.

The CORE-REQ-012 seam audit follows the present data and control flow without inventing the future design. It records legacy parsing through `StageFileParser.parseInteractions`, the `LockDefinition.InteractionLock` representation, existing selector grammar and priority behavior, current single-category compilation through `Schema4StageCompiler.addGenericRules`, and menu enforcement through `AbstractContainerMenuMixin`. It inventories where each `AbstractContainerMenu` click path can increase a destination stack, what authoritative player and destination information is currently available, what mutation or synchronization can occur, and which removal-only or playerless automation paths must remain outside the new gate. It separately records stable block, menu, and inventory-owner catalog availability, `BuiltinEditorSchemas` field definitions, `RulesPanel.tsx` serialization and preservation behavior, production-bundle parity, and all existing parser, compiler, schema, menu, conservation, automation, multiplayer, reload, and custom-menu fixtures. Absence is evidence of a downstream gap, not permission to invent an identity or redesign a legacy interaction.

Reproductions use stable, minimal fixtures and immutable inputs. They record configuration, stage state, player roles, optional-mod set, GUI scale, and artifact identity where applicable. The audit performs no production mutation and requires no publication authorization. Credentials, confirmation codes, private raw logs, personal identifiers, and unrelated environment data are excluded from every retained artifact.

## Failure, Recovery, and Edge Cases

| Scenario | Detection | Required behavior | Recovery or rollback | Regression proof |
|---|---|---|---|---|
| Report lacks an attributable artifact version | Issue record or log omits version identity | Mark classification `blocked_by_missing_evidence`; do not infer the affected release | Request or reconstruct only from authoritative provenance, then append a superseding record | Reviewer sees original blocked row and later evidence identity |
| Source appears fixed but shipped artifact still fails | Source and installed-JAR workflows disagree | Classify from shipped-artifact behavior and record source as a nonconclusive hypothesis | Assign the proven artifact gap to the owning downstream requirement | Re-run installed-JAR workflow from the frozen steps |
| Report cannot be reproduced | Repeated controlled runs differ from the report | Preserve steps, environment, and negative observation; require contradictory artifact evidence before `stale_with_evidence` | Expand only the evidence inputs allowed by the locked report; otherwise remain blocked | Recorded reruns and artifact inspection support the final classification |
| Optional artifact evidence is incomplete | Any EXT-001 typed field is absent | Do not use the artifact to prove supported runtime behavior | Obtain the missing authoritative evidence and repeat the affected reproduction | Evidence register contains all seven typed fields and recalculated hashes |
| Public claim is ambiguous or roadmap-only | Claim lacks attributable 3.0.3 user-visible commitment | Exclude it from CORE-REQ-007 and record the source and exclusion rationale | Escalate only if the owner revises the product contract | Scope review confirms DEC-002 and FUT-001 remain intact |
| Recipe-lock alias or migration is ambiguous | `[recipes].locked` cannot be mapped unambiguously to an output-item or exact-recipe intent | Record the defect and required atomic rejection; do not normalize by guess, delete the draft, or mutate the live file | Preserve the last valid draft, persisted file, compiled snapshot, and synchronized runtime state for CORE-PHASE-003 repair evidence | Issue `#25` record includes before-and-after digests and a repeated failed-apply workflow |
| CORE-REQ-012 is counted as a report | Matrix, completion packet, or release count contains seven issues or invents an issue identity for SRC-009 | Reject the packet; retain exactly six issue rows and one separately typed owner-request seam record | Correct traceability and rerun the issue-count integrity check without changing source-role provenance | Matrix schema proves six report identities and a non-issue SRC-009 record |
| Inventory seam map omits a boundary | No present role or evidence exists for a legacy parser, selector, compiler, menu transaction path, stable target catalog, editor serializer, or required test class | Keep Phase 000 incomplete; record the exact missing source or artifact evidence instead of guessing | Capture the missing seam against the pinned baseline and repeat the Phase 003 handoff review | Reviewer can trace every master-plan CORE-REQ-012 acceptance area to a present seam or explicit absence |
| Reproduction exposes private or secret data | Sanitization review detects credentials, tokens, personal data, or private raw logs | Quarantine the capture and omit it from the packet | Revoke exposed credentials outside this plan if needed and recapture sanitized evidence | Completion review confirms no prohibited data in retained evidence |
| Evidence changes after baseline freeze | Hash, issue revision, documentation revision, or artifact identity differs | Keep the original record and append a superseding evidence entry with reason and timestamp | Re-run only affected classifications and mappings, then invalidate dependent evidence explicitly | Supersession chain identifies every invalidated and renewed proof |
| External prerequisite remains incomplete | EXT-002 unavailable or EXT-003 unauthorized | Preserve `VALIDATED WITH KNOWN EXTERNAL BLOCKER`; do not claim release readiness | Later owning phase satisfies the exact evidence or authorization contract | Phase packet lists blocker state without implying completion |

## Verification Matrix

| Requirement or task | Static or unit | Integration | Real workflow or runtime | Negative and recovery | Evidence artifact |
|---|---|---|---|---|---|
| CORE-REQ-001 | Schema and completeness check for required matrix fields and stable IDs | Cross-check issue, source, documentation, JAR, SRC-009, and implementation-seam identities | Exactly six issue records supported by reproduction, artifact verification, contradictory stale evidence, or an explicit missing-evidence classification, plus one separately typed feature-seam audit | Missing-evidence, stale-classification, and issue-count integrity review | Versioned audit matrix, evidence index, and inventory insertion seam map |
| P000-TASK-001 | Manifest field and hash-format validation | Remote, commit, release, issue, source-role, and owner-decision comparison | Recalculate shipped-artifact hashes | Reject ambiguous or mismatched identity and reject any seven-issue representation | Baseline manifest |
| P000-TASK-002 | Issue-record field validation | Fixture inputs compared with reporter evidence | Repeat each runnable historical workflow and record actual rerun count; use pinned artifact inspection when runtime reproduction is unavailable | Record nondeterminism or missing prerequisites without inferring success or corrected behavior | Exactly six issue records, including the issue `#25` baseline serializer and preservation handoff |
| P000-TASK-003 | JAR inventory, claim-register, source-role, seam-map, and test-inventory completeness | Embedded metadata compared with release provenance; present source roles compared with the master-plan seam list | Inspect the exact shipped JAR and production editor bundle, and trace legacy interaction through parser, compiler, menu, catalog, serializer, and existing tests | Reject rebuilt or unproven substitute artifacts, unstable destination identities, inferred player origin, or guessed absent seams | Artifact inventory, claim register, inventory insertion seam map, and test-boundary inventory |
| P000-TASK-004 | Allowed-classification vocabulary and rationale check | Evidence rows linked to classifications | Repeat decisive workflow for each reproduced or stale result | Force unsupported stale results back to blocked | Issue classification table |
| P000-TASK-005 | Claim-source and scope-boundary validation | Public claim linked to artifact observation | Execute the advertised user workflow where artifact presence alone is insufficient | Exclude novel, roadmap-only, or ambiguous claims | Advertised-capability parity matrix |
| P000-TASK-006 | Stable-ID ownership and orphan check | Map classifications and the SRC-009 feature seam to phase contracts | Confirm the CORE-PHASE-003 handoff is executable without rediscovering ownership | Reject duplicate or missing ownership, issue-count inflation, or a CORE-REQ-012 mapping outside Phase 003 | Acceptance traceability matrix and Phase 003 inventory-insertion handoff |
| P000-TASK-007 | Packet manifest, issue-count, seam completeness, and sensitive-data scan | Cross-artifact identity and external-state review | Independent replay of a representative issue row, claim row, and inventory-seam row | Preserve blocked state for missing proof and append superseding evidence for corrections | Signed or hash-identified completion packet manifest |

Fixtures are the smallest report-specific configurations that preserve the observed behavior: a mixed-player entity-presence scenario for issue `#24`, Curios present and absent states for issue `#8`, JEI and EMI installation and enablement states for issue `#10`, an operator enchantment-rule editor workflow for issue `#11`, a populated stage map at supported GUI scales for issue `#16`, and an operator recipe-output rule workflow for issue `#25`. Phase 000 runs a historical workflow only when the required shipped environment is available. Otherwise it records the strongest pinned artifact observation and uses `blocked_by_missing_evidence` for any unsupported conclusion. For issue `#25`, the baseline record captures the selected intent, actual emitted data, available validation, persistence, and reopen observations, and current parser or compiler interpretation. CORE-PHASE-003 owns the corrected `[recipes].locked_items` round trip, reload, denied and eligible crafting behavior, and failed-apply rollback proof against the 3.0.4 candidate. The SRC-009 inventory-insertion work is not a seventh reproduction fixture. Its Phase 000 evidence is a separately typed current-state seam audit and Phase 003 handoff covering legacy interactions, selectors and priority, menu click paths, player origin and automation, editor catalogs and serializer, and known test gaps. Run identity checks first, classify only the six issues, map downstream ownership, and perform the packet audit. Source inspection and release claims remain labeled lower-fidelity evidence.

## Documentation, Operations, and Release

This phase produces internal execution evidence only: the audit matrix, artifact inventory, claim register, reproduction procedures, classification rationale, SRC-009 source-role record, inventory insertion seam map, existing-test inventory, CORE-PHASE-003 handoff, acceptance traceability matrix, and sanitized completion-packet manifest. It does not edit `README.md`, `DOCUMENTATION.md`, the documentation index, release notes, issue state, or platform metadata. Later phases may update those surfaces only after behavior is implemented and verified. Operations must retain exact artifact hashes, test environment identities, evidence collection dates, and supersession links. No release packaging, tagging, attestation, broker preview, publication, or issue closure occurs in Phase 000.

## Risks and Evidence Invalidation

| Risk | Prevention | Detection | Recovery | Evidence invalidated | Reverification |
|---|---|---|---|---|---|
| Baseline branch or artifact drifts during audit | Pin commit and artifact hashes before inspection | Identity check before every decisive workflow | Stop, preserve old packet, and start a superseding capture for the new identity | All observations after the first mismatch | Repeat identity check and affected workflows |
| Development source is mistaken for shipped behavior | Label every observation by evidence surface | Source and installed-JAR result disagree | Retain source result as hypothesis and rerun shipped workflow | Any conclusion based only on source presence | Installed-JAR or production-bundle proof |
| A report is incorrectly marked stale | Require explicit contradictory artifact evidence and repeated workflow | Reviewer cannot derive stale result from evidence | Restore `blocked_by_missing_evidence` or `reproduced` classification | Stale classification and downstream mapping | Repeat reproduction and independent classification review |
| Audit expands into new feature work | Require public 3.0.3 claim identity for CORE-REQ-007 and keep the sole DEC-007 exception as a separately typed SRC-009 seam record | A capability row lacks an attributable claim, or an owner-request row is not exactly SRC-009 mapped to CORE-REQ-012 | Move unsupported capability rows to excluded evidence with DEC-002 rationale; reject any feature request beyond CORE-REQ-012 | Gap classification, feature-source role, and assigned requirement | Scope audit against DEC-002, DEC-007, FUT-001, and FUT-003 |
| Optional integration evidence is unauthenticated or incompatible | Enforce every EXT-001 typed evidence field | Version, compatibility, provenance, review, or hash field is missing | Replace with authoritative artifact evidence and rerun | Reproduction using the invalid artifact | Artifact identity review and affected workflow |
| Sanitized evidence omits a decisive fact | Keep a field-level redaction note without private content | Reviewer cannot reproduce the conclusion | Recapture a minimal sanitized proof | Classification depending on omitted fact | Independent replay from sanitized inputs |
| External blocker state is overstated | Copy exact availability and authorization values | Packet differs from master prerequisite table | Correct the packet without claiming the prerequisite resolved | Downstream release-readiness conclusion | External-boundary cross-check |

## Phase Completion Packet

The Phase 000 completion packet must contain the baseline manifest; shipped JAR SHA-256 and SHA-512; JAR inventory; public 3.0.3 claim register; sanitized evidence index; exactly six issue records; six-issue classification table; advertised-capability parity matrix; separately typed SRC-009 source-role record; current-state inventory insertion seam map; existing-test inventory; CORE-PHASE-003 inventory-insertion handoff; acceptance traceability matrix; fixture handoffs; external-boundary register; independent consistency review; and a packet manifest that identifies every artifact by digest or immutable source identity. Each issue record states whether its classification rests on reproduction, artifact verification, contradictory stale evidence, or missing evidence. The issue `#25` record must include the shipped editor bundle identity, reporter intent, actual emitted data, available persisted-file digests, validation, reopen, and parser or compiler observations, canonical-field distinctions, and an explicit checklist assigning corrected serialization, reload, runtime crafting, and rollback evidence to CORE-PHASE-003. It must not claim those candidate behaviors were observed in Phase 000. The separate SRC-009 record identifies CORE-REQ-012 and DEC-007, preserves its non-issue status, and traces present parser, model, selector, compiler, menu transaction, destination catalog, editor serializer, production bundle, and regression-test boundaries to downstream acceptance gates. The packet records the baseline commit `5b3077764907249b3711886cca538794f6139acf`, installed artifact, environment versions, configuration identities, evidence dates, classification vocabulary, exclusions, and every `blocked_by_missing_evidence` row.

No corrective source patch, issue closure, release artifact, publication preview, or platform mutation is part of this packet. The packet remains execution evidence outside the protected plan set. Phase 000 may close only when CORE-REQ-001 acceptance criteria pass, every packet identity is reproducible, sensitive-data review passes, every missing historical proof is honestly classified and routed, and downstream phase owners can execute their assigned evidence gates without guessing. The evidence change still follows the repository phase pull request, required checks, review, merge, and signed phase-tag workflow.

## Next Transition

After the Phase 000 completion packet passes its independent consistency review, commit only the approved Phase 000 evidence on the phase branch, run required checks and independent review, merge its pull request through GitHub, verify the resulting `origin/master` contains the merge commit, and create and verify the signed annotated Phase 000 tag on that merged revision. Only then may CORE-PHASE-001 begin from the updated approved `master`, using the entity-presence fixture and issue `#24` mapping from the packet. The SRC-009 seam map remains a frozen downstream contract until sequential execution reaches CORE-PHASE-003 after CORE-PHASE-002. CORE-PHASE-003 consumes it as the current-state boundary for CORE-REQ-012, preserves the three legacy interaction forms, implements the additive paired-selector `item_into_inventory` contract, and owns corrected recipe serialization and candidate runtime proof. Optional-integration and other client or editor work remains governed by its later sequential phase even when its historical evidence was collected here.
